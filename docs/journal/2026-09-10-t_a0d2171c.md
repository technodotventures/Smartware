---
task: t_a0d2171c
board: smartware
status: done
completed: 2026-09-10T15:58:24.000Z
created: 2026-09-10T15:12:41.000Z
assignee: "unassigned"
created_by: "smarty-pants"
artifacts: []
---

# Retention / expiry / archival policy — enforce retention_duration + consume staleness.scope_overrides; scheduled expiry/archive/GC

## Intent

Status: design -> implement -> conformance. RetentionPolicy hooks are DECLARED but UNENFORCED today.

Verified today (2026-09-10):
- RetentionPolicy enum ('forever'|'duration'|'until_revoked') + policy.retention_duration exist on observations (src/layer0/types.ts).
- OBSERVE accepts a retention param (default 'forever'), but every write path sets retention_duration: null.
- staleness config declares scope_overrides, but manifest.ts consumes only default_half_life_days + stale_threshold.

Missing:
- Zero enforcement: nothing reads retention_duration; no expiry scheduler; no archive/cold-storage; no GC/prune.
- No definition of what "expire" means: tombstone vs archive vs purge; time-based vs event-based.

Decision gate (needs explicit approval — protocol/data-model change touching deletion/retention):
- Define expire semantics and composition with FORGET.SCOPE erasure/offboarding, legal-hold, and DSR.
- Define who triggers expiry (host scheduler vs substrate) and the audit marker/receipt.

Acceptance criteria:
1. Spec/ADR: retention + expiry + archival semantics bound, incl. erasure/legal-hold/DSR composition.
2. Enforce retention_duration: an expired observation is archived/tombstoned per policy (never silently deleted without a receipt); recall excludes or flags it.
3. Consume staleness.scope_overrides (per-client).
4. Conformance test: expiry -> archived; recall excludes/flags; evidence retained per policy; deletion receipt written; rebuild-equivalence preserved.
5. Host/Coffee owns the scheduler; substrate provides the idempotent, receipt-backed primitive.

Note: closes the "raw layer grows forever" gap from the bloat analysis — the archival half of the loop.

## Resulting state

_(no completion summary recorded on the board)_

## Provenance

- Canonical record: kanban board `smartware`, task `t_a0d2171c` (`task_events` append-only log).
- Actors recorded: none recorded
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
