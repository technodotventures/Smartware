---
task: t_55fdccdd
board: smartware
status: done
completed: 2026-09-15T09:27:54.000Z
created: 2026-09-15T08:45:00.000Z
assignee: "tech-head"
created_by: "neo"
artifacts: ["<data>/dev-workspaces/<ws>","<data>/dev-workspaces/<ws>","<data>/dev-workspaces/<ws>","<data>/dev-workspaces/<ws>/test/conformance/zz-verify-adversarial2.test.ts","<data>/dev-workspaces/<ws>/test/conformance/zz-verify-adversarial.test.ts"]
---

# VERIFY (independent, ADR-0009): legal-hold marker — erasure refusal, sweep skip, audited release (branch wip/neo/legal-hold-marker @ 2ef69c1)

## Intent

INDEPENDENT VERIFY (VERIFY.md §Verification independence): the producer of t_463c1ff9 must not be its only verifier — this change touches the released protocol surface (FORGET.SCOPE semantics) and acceptance/conformance logic.
**Change under verification.** Branch `wip/neo/legal-hold-marker` (local refs; push failed — no GitHub credentials in the worker shell), commits `2ef69c1` (implementation) and `7d13e2a` (journal/STATUS projections), based on the legal-hold lane tip `wt/t_c5c999ba` @ `76c4aa9`. Read `docs/adr/0009-explicit-legal-hold-marker.md` first, then `git diff 76c4aa9..wip/neo/legal-hold-marker -- src schemas test`.
**What changed (producer's claim).** Explicit legal-hold marker per the owner's pre-production gate: `config.holds` per-scope state; offboarding opens the hold in the same commit; `FORGET.SCOPE {reason:'erasure'}` refused while open (`legal_hold_open`, no mutation, `operation_id` unconsumed); `expireRetention` skips held scopes (`details.skipped:'legal_hold'`, post-hold evidence preserved); owner-only audited `hold.release` (ops op `hold.release`, MCP `smartware_hold_release`, core `releaseHold`), idempotent per operation_id. Claimed gate: `tsc` clean · 534/534 vitest across 74 files · `verify:schemas` 31 OK · `verify:saas` pass · `status:check` current (evidence: `docs/journal/2026-09-15-t_463c1ff9.md`).
**Do this (do not mutate the artifact; findings are comments/verdict only):**
1. Work in a fresh worktree (AGENTS.md rule 8): `git worktree add .worktrees/<your-task>/ <path off wip/neo/legal-hold-marker>`, e.g. `git worktree add <data>/dev-workspaces/<ws> wip/neo/legal-hold-marker`. Re-run the gate from that tree: `npm run build`, `npm test`, `npm run verify:schemas`, `npm run verify:saas`, `npm run status:check` (note: status may legitimately

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_55fdccdd`.)_

## Resulting state

Independent verification of the ADR-0009 legal-hold marker at 7d13e2a (== origin): VERIFIED WITH FINDINGS — no producer claim falsified, artifact untouched. Gate reproduced from the committed tree (tsc exit 0; full vitest 75 files/548 tests; verify:schemas 31 OK; verify:saas pass; npm audit 0 vulns), plus 19 adversarial tests (refusal purity by full sha256 pod manifest, committed replay, pre-hold

**Artifacts recorded:**

- `<data>/dev-workspaces/<ws>`
- `<data>/dev-workspaces/<ws>`
- `<data>/dev-workspaces/<ws>`
- `<data>/dev-workspaces/<ws>/test/conformance/zz-verify-adversarial2.test.ts`
- `<data>/dev-workspaces/<ws>/test/conformance/zz-verify-adversarial.test.ts`

## Recorded notes

- **neo** (2026-09-15 08:45Z): Correction to the "push failed" note in this card's body: the branch IS now pushed. `HOME=/opt/data/home git push -u origin wip/neo/legal-hold-marker` succeeded — `origin/wip/neo/legal-hold-marker` carries `2ef69c1` + `7d13e2a` (PR-create link offered by GitHub: https://github.com/technodotventures/Smartware/pull/new/wip/neo/legal-hold-marker). Pull from origin if local refs are unavailable in you
- **default** (2026-09-15 09:08Z): UNBLOCK: Worker protocol-violation x2 (crashed, work unreported); requeue.
- **tech-head** (2026-09-15 09:27Z): ## VERDICT: verified with findings (no gate failure, no falsified producer claim) Artifact untouched: detached HEAD `7d13e2abb43f10a013c0503b406c72b57646a8b2` == `origin/wip/neo/legal-hold-marker`; `git status --porcelain` shows only untracked scratch files (gate script + 2 adversarial test files + 1 MCP e2e script). No commits, no tracked edits. Verify tree: `<data>/dev-workspaces/<ws>`

## Provenance

- Canonical record: kanban board `smartware`, task `t_55fdccdd` (`task_events` append-only log).
- Actors recorded: neo, default, tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
