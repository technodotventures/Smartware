---
task: t_85817375
board: smartware
status: done
completed: 2026-09-15T10:09:15.000Z
created: 2026-09-15T08:39:02.000Z
assignee: "tech-head"
created_by: "neo"
artifacts: []
---

# L1 record writer: insertClaim stamps op_LEGACY00000000000000000000, which the published OperationId pattern rejects

## Intent

## Why
Adjacent finding from `t_9e124fe6` (tombstone writer), measured there but **not fixed** — that card's
scope guard excludes `src/layer1/store.ts`. Same class as `t_2bba749f`/`t_9e124fe6`: a library writer
emits a value the published contract rejects.
`src/layer1/store.ts:486`:
```ts
const opId = operationId ?? 'op_LEGACY00000000000000000000';
```
`insertClaim` stamps this into the canonical L1 record (`pod_data/claims/<YYYY-MM>.jsonl`) whenever
the caller supplies no operation id. `schemas/v0.5.0/claim.schema.json` requires `operation_id`
(`$ref` `common.schema.json#/$defs/OperationId`), pattern `^op_[0-9A-HJKMNP-TV-Z]{26}$` — Crockford
base32, which excludes I, L, O, U. The placeholder is 26 chars after `op_` but contains `L`, so the
value is rejected by the contract the library publishes.
## Measured (2026-09-15, Node v26.5.1, worktree of `t_9e124fe6`)
Probe: store with `setDataDir` → `insertClaim` a claim with `operation_id: null` → read the JSONL

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_85817375`.)_

## Resulting state

Reviewed and APPROVED (round 1, artifact lens) at wip/smarty/l1-legacy-op-id @ 0a68482 (base 52efe97). insertClaim's legacy fallback now stamps the Crockford-valid LEGACY_OPERATION_ID = op_000000000000000000000000A3, the same marker tombstone-backfill.ts uses; I re-derived every claim rather than trusting the handoff — RED on the pre-fix tree 4/6 with exactly the /operation_id pattern error, GREEN

## Recorded notes

- **default** (2026-09-15 09:08Z): UNBLOCK: Worker pid died; requeue.
- **smarty-pants** (2026-09-15 09:42Z): **Handoff note for the reviewer (tech-head)** — decision, evidence, and what I deliberately did NOT do. **Delivered.** `wip/smarty/l1-legacy-op-id` @ `0a68482`, base `52efe97` (the card body's `store.ts:486` matches that base exactly). 4 files, +278/−10: `src/layer1/store.ts`, `test/layer1/legacy-operation-id.test.ts` (new, 6 tests), `docs/conformance-status.md`, `docs/STATUS.md` (regenerated proj
- **smarty-pants** (2026-09-15 09:44Z): **State note (implementer session, smarty-pants) — no further board transition from this session.** Run `101` (profile smarty-pants) already ended with `outcome: review_requested` at 09:42:10 and is the implementer's terminal call for this card. The card then moved to `review` and the dispatcher claimed it for run `103` (profile `tech-head`, pid 1054143, started 09:42:31) — that run is still in fl
- **tech-head** (2026-09-15 10:09Z): **Review round 1 (artifact lens) — APPROVED.** Deliverable `wip/smarty/l1-legacy-op-id` @ `0a68482` (base `52efe97`); implementer's worktree read-only (clean at the tip). Full evidence attached: `t_85817375-review-evidence-compact.txt` (sha256 `353d10fa…`, re-verified after storing) and `retention-opid-evidence.tgz` (`f15ae2a0…`). **Re-derived, not trusted from the handoff (all in throwaway detach

## Provenance

- Canonical record: kanban board `smartware`, task `t_85817375` (`task_events` append-only log).
- Actors recorded: default, smarty-pants, tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
