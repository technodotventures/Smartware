---
task: t_9e124fe6
board: smartware
status: done
completed: 2026-09-15T08:47:02.000Z
created: 2026-09-15T08:09:51.000Z
assignee: "tech-head"
created_by: "smarty-pants"
artifacts: ["/opt/data/rev-t9e124fe6/review-evidence-compact.txt"]
---

# Tombstone writer: tombstone-backfill.ts emits snapshots the published schema rejects (4 required fields missing, confidence not bucketed)

## Intent

## Why
Flagged (not fixed — explicitly out of scope) by `t_2bba749f`, which fixed the *schema* side of the
tombstone snapshot contract. The other side is still wrong: the only in-tree writer of
`wiki/tombstones/*.md` emits frontmatter that the published schema rejects.
`src/layer1/tombstone-backfill.ts` → `buildTombstoneFromLegacyClaim` builds the `snapshot` object
literal by hand. It omits four fields the snapshot block lists as `required` — `claim_id`, `state`,
`epistemic_owner`, `fingerprint` — and writes `confidence` as the raw numeric claim confidence
rather than a `ConfidenceBucket` (`'high' | 'medium' | 'low'`). `Claim` (`src/layer1/types.ts`) has
no `fingerprint` field at all, so the writer cannot emit one without a decision.
## Measured (2026-09-15, `t_2bba749f`)
Probe drove `backfillTombstones(store, wikiDir)` over one legacy `status: 'retracted'` row
(fixture ids pattern-valid, so the errors are the writer's, not the fixture's), parsed the written
frontmatter, validated it against `schemas/v0.5.0/tombstone-frontmatter.schema.json`:
- `{"retracted_found":1,"tombstones_written":1,"tombstones_skipped_existing":0}`
- **valid: false** — 6 Ajv errors: `required` missing `claim_id`, missing `state`, missing

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_9e124fe6`.)_

## Resulting state

Reviewed and APPROVED (round 1, artifact lens) at wip/neo/tombstone-backfill-writer @ 52efe97 (fix 35f34d2, base fbce04d), reproduced in a fresh reviewer worktree. The writer now emits frontmatter the published tombstone schema accepts, and I re-derived that myself rather than trusting the handoff: restoring the pre-fix writer fails the new test 2/3 with exactly the card's six Ajv errors plus two

**Artifacts recorded:**

- `/opt/data/rev-t9e124fe6/review-evidence-compact.txt`

## Recorded notes

- **tech-head** (2026-09-15 08:17Z): **Evidence from the review of `t_2bba749f` (tech-head, run 71)** — the sub-question this card raises about `fingerprint` and `confidence` already has in-repo precedents. Not a decision, and not binding on `neo`; pointers only, so a second mapping is not invented: - **`fingerprint` for a legacy row — recompute, and there is already an implementation + rule for it:** `src/layer1/migration.ts:14` `ba
- **neo** (2026-09-15 08:40Z): **Lifecycle note (neo, run 75, 2026-09-15 ~08:40 UTC).** This run ended with `kanban_request_review` at 08:39: the board reads `status=review`, assignee `tech-head`, run 75 `outcome=review_requested`, `current_run_id: null`. A watchdog notice then arrived claiming the task was "still running" and demanding `kanban_complete`/`kanban_block`. I re-attempted the review handoff to satisfy it and the ke
- **neo** (2026-09-15 08:41Z): **Terminal-transition record (neo, run 75, ~08:42 UTC).** The orchestrator's watchdog has twice demanded a worker-side `kanban_complete`/`kanban_block` for this card. Both are unavailable, and I can now show that with kernel replies rather than an assertion: - `kanban_request_review` (re-issued to satisfy the notice) → *"could not request review for t_9e124fe6: task is not in running/ready (or exp
- **neo** (2026-09-15 08:42Z): **Verified close (neo, run 75).** Board read-back via the shared DB: run 75 (neo) ended 08:39:13 with `review_requested`; the review dispatch was picked up — **run 78 (tech-head) spawned 08:39:56 and is live** (heartbeating). That is why the task still reads `running`: the active run is the reviewer's, not mine, and it is why a worker-side `complete`/`block` from this closed run is rejected. The l
- **tech-head** (2026-09-15 08:46Z): **Review (tech-head, run 78) — round 1 (artifact lens) — verdict: APPROVE.** Evidence attachment `t_9e124fe6-review-evidence-compact.txt` (sha256 `9bea9e87…fedc`, verified after storing); full logs `/opt/data/rev-t9e124fe6/{gate,probe}.log`. Reviewed in a fresh detached worktree at `52efe97`, not the implementer's tree; the worktree is released and the deliverable was not edited by the reviewer. W

## Provenance

- Canonical record: kanban board `smartware`, task `t_9e124fe6` (`task_events` append-only log).
- Actors recorded: tech-head, neo
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
