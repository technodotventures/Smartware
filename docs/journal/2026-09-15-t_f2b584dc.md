---
task: t_f2b584dc
board: smartware
status: done
completed: 2026-09-15T11:15:00Z
created: 2026-09-15T10:54:59Z
assignee: "tech-head"
created_by: "neo"
artifacts: ["docs/adr/0012-grant-granularity-at-the-forget-scope-boundary.md","docs/integration/coffee-adapter.md","docs/competitive/mem0-substrate-spec-draft.md","/opt/data/workspaces/brain-pilot-evidence/adr-0012-20260915T110900Z/NOTES.md"]
---

# DECIDE: the revocation boundary is the grant row — one row per (actor, client), and the re-grant requirement (ADR-0012)

## Intent

Decide the ADR-0011 §6 finding `grant-row-revocation` (gate run card `t_9740ae98`,
checks 6a/6c/6n): `FORGET.SCOPE` revokes every grant **row** whose capability arrays
reference the scope (spec §10b.3), and the adapter has no re-grant surface — so in
the Coffee tenant model (one grant per actor listing all their clients) erasing one
client silently removes that staff member's access to their **other** clients.
Three options were on the card: (a) keep row-scoped revocation and make provisioning
narrow (per capability/scope), (b) narrow the revocation in the substrate, (c) add
an adapter re-grant path. Deliverable for (a): the written decision, the conformance
tests it implies, and the doc/handoff text change — **no implementation beyond that**.
Out of scope: substrate behaviour changes, and the published protocol surface.

## What was done

- **Decided (a)** and recorded it as **ADR-0012** (`docs/adr/0012-grant-granularity-at-the-forget-scope-boundary.md`,
  Status: Accepted): the substrate keeps row-scoped revocation exactly as released;
  provisioning issues **one grant row per (staff actor, client scope)**, so the
  revocation boundary equals the intended access boundary by construction.
- **Measured the decision instead of asserting it** (`scripts/scratch-f2b584dc-measure.mjs`,
  deleted from the tree; kept in the evidence dir as `measure.mjs`; raw output
  `raw.log`, facts `measure.json`). On the **public adapter + package surface**,
  Node v26.5.1, in-process:
  - **S1 — shipped shape** (one row, `sam[meridian, arcadia]`): erasing `arcadia`
    → `grants_revoked: 1`, the row is `status:'revoked'` with **both** scopes in its
    arrays, and sam's `meridian` recall flips `ok:true n:1` → `ok:false 403
    insufficient_permission`. Re-derives gate check 6n independently.
  - **S2 — decision shape** (one row per client): erasing `arcadia` → `grants_revoked`
    = exactly the arcadia row; the meridian row stays `status:'active'`; sam's
    `meridian` recall is `ok:true n:1` **with no re-provisioning**; `arcadia` is
    `403`. The property the decision buys, measured.
  - **S3 — the sharp edge the shape exposes**: with two rows, a **standby** replica's
    config-derived precheck (`#precheck`, `Array.find()`) answers `ok` for the first
    row's client and `403 insufficient_permission` for the **second** row's client —
    an actor who holds a row for it. Fail-closed but wrong; named as a required
    companion change.
- **Doc/handoff text changed** (the (a) deliverable):
  - `docs/integration/coffee-adapter.md` §7 — new *Provisioning grants* block: the
    per-client row shape, the **re-grant requirement** (a host that keeps a
    multi-client row must re-provision the remaining scopes in the same
    offboarding/erasure step; re-granting is a config provision, never a protocol
    call), the degraded-precheck union requirement, and the honest status that the
    shipped `coffeeTenantConfig`/`#precheck` still need the change. Also §1's grant
    bullet and the `forgetClientScope` row (row-scoped, "a row must carry exactly
    one client").
  - **Normative text** (`docs/competitive/mem0-substrate-spec-draft.md` → v0.15):
    §10b.1 `grants[]`, §10b.2 (grant-granularity binding bullet + first bullet),
    §10b.3 (row-scoped pointers), §10b.4 (grant-shape note: the worked example
    still shows the single-row shape), drafting-history entry.
  - **Handoff paragraph**: the verbatim sentence for the Coffee release handoff is
    in ADR-0012 §"Handoff text"; a comment on card `t_46c4acce` points at it.
- **Conformance tests the decision implies** are specified in ADR-0012 §6 as a table
  (C1–C5: gate `6n` inverted to the positive invariant + a provisioning-shape check,
  the adapter smoke's degraded-path union check, the unit-level two-row revocation
  case, and the example-config/shape-harness re-verification). **Not implemented
  here** — the card scopes (a) to the decision, the doc text and the tests it
  *implies*; implementation is card `t_864a5900` (created by this card, assigned to
  the release line's owner), which must land before the release candidate is packed.

## Evidence

```
Gate:   node scripts/scratch-f2b584dc-measure.mjs  → 16 facts, exit 0; re-run reproduced all 16
        (evidence: /opt/data/workspaces/brain-pilot-evidence/adr-0012-20260915T110900Z/)
Gate:   node scripts/scratch-f2b584dc-session.mjs  → 24 facts, exit 0; re-run reproduced all 24
        (only fresh ULIDs differ) — round 2, S4, on the public package surface
Gate:   npm run build                → tsc clean (exit 0)
Gate:   npm test                     → 551/551 vitest passed (284.17s: transform 20.29s,
                                       import 45.57s, tests 208.35s) on this 2-vCPU box
Gate:   npm run verify:schemas        → Verified 31 schema files across v0.4.2, v0.5.0
Gate:   npm audit --omit=dev --audit-level=low → found 0 vulnerabilities
Gate:   npm run verify:saas           → SMOKE_OUTCOME=pass
Gate:   npm run verify:coffee-adapter → 53/53 passed · ADAPTER_SMOKE_OUTCOME=pass
Gate:   npm run status                → wrote docs/STATUS.md (tree e937fab on wt/t_f2b584dc)
Gate:   npm run status:check          → STATUS.md is current.
Gate:   npm run journal:sync          → 64 completed task(s): 5 written, 59 already present
NOT RUN: npm run verify:coffee-gate   → deliberately: this change is docs-only, the fixture is
                                       untouched, and its check 6n currently *asserts* the
                                       finding this card decides (that assertion is inverted by
                                       card t_864a5900). Re-running it would re-assert the defect.
Run:    2026-09-15, 2 vCPU (shared with other lanes), Node v26.5.1, n=1 for the measurement
        (deterministic, in-process, no Redis/network); n=1 for the suite
Notes:  The measurement proves the mechanism and re-derives the gate finding; it does
        NOT prove staging behaviour, concurrency, or the adapter fixes (not yet
        implemented). The first-match hazards now split two ways, and the split is
        measured rather than described: the SESSION path
        (`src/session/policy.ts:96` — capabilities AND trust/quarantine caps from the
        first active row) is MEASURED (S4, 24 facts: per-client rows → a session for
        the second row's client reports `capabilities_granted: []` while the adapter's
        recall on that scope answers `ok:true n:1`; the same two rows swapped give
        `user_facing` vs `verified`), while `ensureTrustedClientGrant` and
        `evaluateAccess`'s deny-reason text remain READ FROM THE SOURCE, not measured.
        Stated the same way in
        /opt/data/workspaces/brain-pilot-evidence/adr-0012-20260915T110900Z/NOTES.md
        §"What this does NOT prove".
```

## Round 2 — review verdict `changes_requested` (what changed, and why)

The artifact review (card `t_102f3dfa`, round 1) cleared the decision, the
evidence and the handoff text, and blocked on one gap: the deliverable's sharp-edge
inventory was incomplete and the new §10b.2 rule ("first-match lookups are never an
authorization decision") was false as written.

- **The gap, re-measured here rather than taken on trust (S4, 24 facts, reproduced
  twice):** `src/session/policy.ts:96` (`resolveTrust()` → `getGrantForActor()` →
  `resolveCapabilities()`) is a **third** first-match site, on the public MCP
  surface (`smartware_session_start`, `src/mcp.ts:560`). With one row per client, a
  session requesting the **second** row's client returns `capabilities_granted: []`
  while `checkGrant` authorizes the actor on that scope and the adapter's recall on
  it answers `ok:true n:1`; caps and trust both follow the first row, so the same
  two rows swapped give `user_facing` vs `verified`. S4 also found two things the
  review's probe did not: omitting `requested_scopes` **masks** the defect (the
  default is every config scope, and the first row covers one of them — why it
  survived round 1), and a `quarantine: true` row that grants the requested scope is
  ignored by the session (S4E).
- **Routing decision (the review's correction 2, option (i)):** the session union is
  folded into `t_864a5900` as **companion change 3**, not deferred. The reason is
  causal: the per-client shape is what makes the session surface wrong, so shipping
  the shape without the union would introduce a known-wrong public surface. The fix
  is single-row-invariant (one row resolves exactly as today), so the released
  session surface — protocol contract v0.4.1 — does not move for conformant configs;
  `t_864a5900`'s "the substrate is NOT changed" preamble is amended for exactly this
  call site by a comment on that card.
- **Semantics decided here, not left to the implementer** (ADR-0012 §4): a
  capability is granted if **any** active row of the actor covers **any** requested
  scope; trust/quarantine take the **most restrictive** union across the actor's
  active rows. Provisioning must additionally keep `trusted`/`quarantine` uniform
  across an actor's rows (contract §7) — the restrictive union is the fail-safe for
  divergence, not a licence to mix.
- **Correction 3 (make the rule true or qualify it):** §10b.2's sentence now names
  its one live exception, the site, and the card that closes it; the journal's
  read-from-the-source list is split accordingly (the session path is now **measured**
  — S4, not asserted) and the same split is written into the evidence NOTES.md.
- **Correction 2's "name it in §Consequences" is done with the routing attached:**
  the sharp-edge bullet separates the live violation (session path, measured, folded
  into `t_864a5900`) from the read-from-source hazards (`ensureTrustedClientGrant`,
  `evaluateAccess`'s deny reason), so the text can no longer read as "audited and
  clean".
- **Non-blocking note closed as well (review §"Non-blocking notes", first bullet):**
  revocation matches the scope by exact `list.includes()`, authorization by
  `scopeMatches()` — a `*` row authorizes a scope it is **not** revoked with. One
  sentence in §10b.3 (exact-id revocation matching, unreachable for conformant
  configs) and one added assertion in C2 (no active row carries `*`). Substrate
  behaviour unchanged.
- **Not touched, on purpose:** the ADR-number collision (`docs/adr/0012-*` minted by
  three lanes) is carded as `t_201cdca8` and owned by its own card; PR #16 stays
  open and unmerged until the number is settled. Nothing in this round depends on it
  beyond the file name.

Amended files this round: `docs/adr/0012-…md` (§4 companion 3, §5, C2/C6, §Consequences,
§Evidence), `docs/competitive/mem0-substrate-spec-draft.md` (§10b.2 exception, §10b.3
exact-id note, v0.15 round-2 amendment), `docs/integration/coffee-adapter.md` §7
(session union + host obligations), `VERIFY.md` (round-2 recorded row), this journal, and
the evidence directory (`measure-session.mjs`, `measure-session.json`, `session-raw.log`,
`session-raw-run2.log`, NOTES.md).

Gate (round 2 — docs-only: the diff is markdown only, no `src/`, `test/` or schema file):

```
Gate:   node scripts/scratch-f2b584dc-session.mjs → 24 facts, exit 0; re-run reproduced all 24
                                     (only fresh ULIDs differ)
Gate:   npm run build             → tsc clean (exit 0)
Gate:   npm test (run 1)          → 546/551, 2 files red: test/conformance/mcp_smoke.test.ts
                                     (stdio hook timeout 10000ms) and test/regressions.test.ts
                                     (fixture hook) — box at load average ~25
Gate:   npm test -- <those two>   → 51/51 passed (235.77s) in isolation on the same tree
Gate:   npm test (run 2)          → 546/551 again, 1 file red: test/conformance/mcp_smoke.test.ts
                                     (5 tests, 10–12s each) — box at load average 32 (2557s wall)
Gate:   npm run verify:schemas    → Verified 31 schema files across v0.4.2, v0.5.0
Gate:   npm audit --omit=dev --audit-level=low → found 0 vulnerabilities
Gate:   npm run verify:coffee-adapter → 53/53 passed · ADAPTER_SMOKE_OUTCOME=pass
Gate:   npm run verify:saas       → SMOKE_OUTCOME=pass
Gate:   npm run journal:sync      → 4 entries written (69 total)
Gate:   npm run status            → wrote docs/STATUS.md; status:check → "STATUS.md is current."
                                     once, immediately after regeneration, and STALE on every other
                                     attempt. Mechanism, read from the projector and then measured:
                                     `--check` is an exact-content match against a fresh projection
                                     (it ignores only working-tree cleanliness and the tree hash,
                                     scripts/substrate-status.mjs:308-320), so any live-board field
                                     that moves between write and check fails it. Measured: the first
                                     check (before regenerating post-journal:sync) drifted on two
                                     lines, both live state — journal entries 65 → 69 (the four
                                     entries journal:sync itself owed) and events 3814 → 3823; after
                                     that, the only drifting line is the event counter — 4230 → 4253
                                     in the ~3 minutes between a regeneration and the re-check. On a
                                     board with other lanes heartbeating, the projection is a
                                     snapshot, not a lock — the same class of live-state drift the
                                     round-1 review identified (it found 14 such lines, all live
                                     state, no content line).
NOT RUN: npm run verify:coffee-gate → deliberately, as in round 1: docs-only, and its check 6n still
                                     asserts the finding this card decides (inverted by C1,
                                     t_864a5900)
```

The vitest red is the box, not this change, and it is not written off silently: the
diff touches no code path, and the files involved pass in isolation on the same tree.
A full-suite run on a quiet box is the clean signal still owed — no claim in this
card depends on it.

## Round 3 — the review's second pass (`changes_requested`): the retired-marker rule reaches the procedure

The independent review card `t_102f3dfa` (round 2) cleared the decision and
reproduced every measured claim on a clean `e937fab` worktree (S1/S2/S3 16/16 and
S4 24/24 facts identical after ULID normalization; handoff paragraph verbatim;
diff docs-only) and required **three corrections, all text/assertion-level, none
needing re-measurement** (comment 169 +
`attachments/t_102f3dfa/REVIEW-t102f3dfa.md`). They are carried by card
`t_ff85ae12`, which is now also a **parent of `t_864a5900`**: the implementation
must start from this text, because C7 is part of its acceptance spec.

- **C1 (blocking) — the operational procedure did not carry the retired-marker
  rule.** Spec §10b.2 line 133 already binds it ("never issue `*`, never re-list a
  retired marker") and the rule needed no substrate change, but contract §7's
  re-grant procedure — the text the Coffee engineer implements against — said only
  "re-provision the actor's remaining scopes", and the obvious edit on the surface
  §7 sends an operator to is the trap. Measured by the review (R4/R5) and re-run
  here first-party: after an `erasure` the revoked row still lists the retired
  `client:arcadia#1`, and re-activating it re-authorizes the purged scope
  (`checkGrant` true, `handleWrite` **201**, owner recall `ok:true`, standby
  degraded `ok:true n:1`; control without the restored row `403`). §7 now states the
  rule — live scope ids only, a **fresh** row (or edit the revoked row's arrays
  first), never re-activate a row that still names `client:<id>#n` — and separates it
  from `offboarding`, where re-activation **is** the sanctioned revival path
  (protocol v0.5.0 §FORGET.SCOPE: "revoked but re-activatable"). ADR-0012 §3 carries
  the same rule with the measured hazard and the R-series evidence.
- **C2 (blocking) — the assertion.** ADR-0012 §6 gains **C7**: on the packaged
  fixture, after the erasure check 6c already performs, **no active row references
  the removed scope id**. Same class of provisioning hygiene C2 closes for `*` rows;
  the retired-marker case fails open today (R5).
- **C3 (blocking, minor) — handoff fitness.** (i) the handoff paragraph's
  present-tense "the reference adapter provisions one grant row per (staff member,
  client)" was false of the shipped template until `t_864a5900` lands (measured
  `row_count: 1`, R0) — it is now qualified with the card and the shipped shape, as
  contract §7 already did; (ii) the sequencing note on card `t_46c4acce` is
  refreshed to **three** companion changes (the adapter's per-client row shape, the
  degraded-precheck union, and the session resolver union measured in round 2) plus
  tests C1–C7, so a release card cannot pack with the session defect unfixed.

Amended files this round: `docs/adr/0012-…md` (§3 second paragraph, §6 C7, §Evidence
round-3 bullet, handoff paragraph), `docs/integration/coffee-adapter.md` §7 (retired
marker + the measured re-grant mechanism), `VERIFY.md`, `docs/STATUS.md`, this
journal, and the evidence directory (`measure-regrant.mjs`, `measure-hazard.mjs`,
`regrant-raw.log`, `hazard-raw.log`, `*-facts.json`, `normalize_check_round3.py`,
`normalize-report-round3.txt`, NOTES.md §R).

Gate (round 3 — docs-only, and the same class of run as round 2):

```
Gate:   node scripts/zz-round3-regrant.mjs  → 17 facts, exit 0 (evidence copy:
        measure-regrant.mjs) — every fact identical to the review's rerun-regrant-raw.log
        after id/ULID normalization
Gate:   node scripts/zz-round3-hazard.mjs   → 7 facts, exit 0 (evidence copy:
        measure-hazard.mjs) — identical to hazard-raw.log after normalization
Gate:   python3 normalize_check_round3.py   → 17/17 and 7/7 identical; report in
        normalize-report-round3.txt
Gate:   npm run build                       → tsc clean (exit 0)
Gate:   npm test                            → **551/551 passed, 77 files, 83.80 s** (exit 0) —
        the quiet-box full-suite run rounds 1–2 left owed; the load-induced stdio-hook
        reds of round 2 do not reproduce
Gate:   npm run verify:schemas              → Verified 31 schema files across v0.4.2, v0.5.0
Gate:   npm audit --omit=dev --audit-level=low → found 0 vulnerabilities
Gate:   npm run verify:coffee-adapter       → 53/53 passed · ADAPTER_SMOKE_OUTCOME=pass
Gate:   npm run verify:saas                 → SMOKE_OUTCOME=pass
Gate:   npm run status                      → wrote docs/STATUS.md (tree e937fab base, branch
        wt/t_f2b584dc); status:check → STATUS.md is current
Gate:   npm run status:check (≈4 min later)  → STALE on exactly ONE line — the live board-event
        counter (8202 → 8205 events; tasks unchanged at 96), the same live-state class round 2
        identified. Mechanism re-read: `--check` is an exact-content match that normalizes only
        the working-tree note and the tree hash (scripts/substrate-status.mjs:308-320). STATUS.md
        was regenerated after every other writer in this round (journal:sync included) and is
        committed current as of its own generation; no content line has ever been the drift.
Gate:   npm run journal:sync                → 76 completed task(s): 7 written, 69 already present
NOT RUN: npm run verify:coffee-gate         → deliberately, as in rounds 1 and 2: docs-only,
        and gate check 6n still asserts the finding this card decides (inverted by C1, and
        C7 is added, by t_864a5900)
Run:    2026-09-16, 2 vCPU (load average 0.8–3.5), Node v26.5.1, n=1 per command
```

## Resulting state

The finding has a decision instead of a warning: ADR-0012 (Accepted) is the binding
record, §10b binds the per-(actor, client) row shape, the adapter contract states the
re-grant requirement and the degraded-precheck union, and the Coffee handoff text
exists verbatim for card `t_46c4acce`. The published protocol surface and the
substrate's revocation semantics are unchanged, so the ADR-0011 gate evidence (79/79)
still stands. Open work: card `t_864a5900` (adapter shape + precheck union + the session
resolver union + C1–C7 tests — C7 being the retired-marker assertion added in round 3 —
plus the example/harness re-shape).

## Unresolved

- The reference adapter still provisions one row per actor and its degraded precheck
  is first-match until `t_864a5900` lands — stated in the contract doc, so the
  operative rule for the Coffee pilot today is the re-grant requirement.
- **The re-grant procedure's retired-marker trap is now named but not yet asserted
  in the fixture.** An operator who follows the *old* obvious edit — re-activating
  the revoked row an `erasure` leaves behind — re-authorizes the purged scope
  (measured R4/R5: `checkGrant` true, write 201, recall `ok:true`, standby degraded
  `ok:true n:1`; control 403). Contract §7 and ADR-0012 §3 state the rule and
  protocol v0.5.0 §FORGET.SCOPE keeps the mechanism closed for `offboarding`
  (revoked-but-re-activatable) — but the runtime validates no scope ids (§10b.5),
  so until C7 lands with `t_864a5900` the guard is provisioning discipline plus the
  gate's existing checks, not a test.
- **The session surface is the last known first-match derivation** and it is wrong
  under the shape this ADR makes binding: `smartware_session_start` reports
  `capabilities_granted: []` for a client held in a second row (measured, S4). It is
  folded into `t_864a5900` (companion change 3 of §4, test C6 of §6) rather than
  deferred; until that card lands, a host using sessions **and** per-client rows must
  not read an empty capability list as a denial (contract §7). The Coffee adapter
  itself never calls this surface.
- Options (b) and (c) are recorded with their costs in ADR-0012 §Alternatives; (b) is
  a **released** protocol-surface change and is an owner escalation, so it could not
  be taken by this card even had it been the better trade.
- `docs/competitive/coffee-tenant-config.example.json` and §10b.5's "code-verified"
  facts still describe the single-row example; the divergence is written down in
  §10b.4 and in ADR-0012 §5, and moves with `t_864a5900`.
