---
task: t_8f2c028a
board: smartware
status: done
completed: 2026-09-15T12:57:09.000Z
created: 2026-09-15T11:36:08.000Z
assignee: "tech-head"
created_by: "neo"
artifacts: []
---

# VERIFY (independent): host-lane/actor-id lane — branch wip/neo/host-lane-identity @ 9114fb7 (base wip/tech-head/claim-record-semantic @ 981e5a7)

## Intent

## What to verify
Independent verification of the lane for kanban `t_9a700aed` (carded out of `t_229601e4`). Repo:
`<checkout>/smartware`, worktree `.worktrees/t_9a700aed`; branch
`wip/neo/host-lane-identity` @ `9114fb7` (change `42c37ed`), base `wip/tech-head/claim-record-semantic`
@ `981e5a7`; PR technodotventures/Smartware#17 (OPEN, stacked, unmerged). **Owner approval of ADR-0012
is the merge gate — nothing here authorizes a merge.**
Deliverable under audit:
1. **Writer identity fix** — `substrateActorId(config)` (`src/config.ts`) wired into
`src/protocol/reflect.ts`, `src/compile_queue/worker.ts` (public `podActorId` delegates) and
`SmartwareCore.dream`. Claim: one instance mints one conformant lowercase `substrate:<slug>`
(`smartware_coffee` → `substrate:coffee`; ULID instances → lowercased ULID), where before the fix
reflect.auto/the compile queue wrote `substrate:<UPPER ULID>` (pattern-rejected) and dream a second
spelling.
2. **The disclosure** — `schemas/v0.5.0/README.md` (new "Scope vocabulary" section),
`docs/conformance-status.md` → *Remaining limits* + verified-baseline entry, `createPodProfile`

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_8f2c028a`.)_

## Resulting state

VERDICT: PASS on all three audited claims at wip/neo/host-lane-identity @ 9114fb7 (PR #17, unmerged; owner gate untouched, branch and both verifier worktrees unmodified). Claim 1 (writer identity): fixed and independently re-derived — my own raw-JSONL+Ajv harness over a named AND a default-ULID instance across all three writers (sync reflect.auto, drainCompileQueue, dream) shows base = two identit

## Recorded notes

- **neo** (2026-09-15 11:38Z): Cosmetic fix to the body above: the *"The pins"* list was truncated in transit. It reads: `test/layer1/pod-profile-conformance.test.ts` (new, 4 tests), `test/schemas-v0.5.0.test.ts` (+1 closed-vocabulary fixture), `test/semantic-materialization.test.ts` (actor-id substitution removed — only the host-lane scope is substituted now). Nothing else in the card is affected.
- **tech-head** (2026-09-15 12:56Z): ## Verdict — PASS on all three audited claims (audited revision `9114fb7`), with one record defect carded Independently re-derived in fresh detached worktrees (`wt-tip` @ `9114fb7`, `wt-base` @ `981e5a7`), each with its own `npm ci`. Nothing committed or pushed; both worktrees clean at the end, `common.schema.json` back to `7949e3d1…`, `git rev-parse` for the audited revision unchanged. **1. Write

## Provenance

- Canonical record: kanban board `smartware`, task `t_8f2c028a` (`task_events` append-only log).
- Actors recorded: neo, tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
