---
task: t_a6bf30a8
board: smartware
status: done
completed: 2026-09-17T03:18:51Z
created: 2026-09-17T02:36:09Z
assignee: "tech-head"
artifacts: ["/opt/data/workspaces/brain-pilot-evidence/catchup-lane-sync-20260917T025418Z/"]
---

# The catch-up re-syncs the claim-FTS lane: `replayCatchUp` reports the scopes it materialised, and every call site that owns a lane repairs them

## Intent

Fix the measured instance of the survey's class (`t_12c79071`) that `t_8779781f` routed here: **a
mid-session `replayCatchUp` materialises claim rows without re-syncing the claim-FTS lane**, so live recall
misses them until some re-syncing write happens while every restart serves them. The card named the
mutator ("not a verb's own logic"), three verbs that carry it, a suggested shape, and the reachability
caveat it wanted stated here. It also asked for the decision to be made rather than taken, a RED probe
first, one journal entry, and the full VERIFY.md gate for the `src/` change.

Out of scope, and left where it was: the demotion-durability work (`t_01ef0ede`/`t_742e31f9`,
`e5f093e`/`3ae8a6b`) — not on this branch's base, but **already contained in the local-only
`integration/v0.8.0` head** today (measured; see *Base and containment*) — the survey's per-verb repairs
that are not on this base (REVISE, PR #29), FORGET's settled keep-rows design (`t_8779781f`), and the
pre-ADR-0016 log repair (`t_a54a4606`).

## Base and containment (re-checked first, as asked)

`wip/neo/demotion-durability` @ `e5f093e` is contained in `wip/smarty/demotion-handbuilt-records` @
`3ae8a6b`, and **both are already ancestors of the (local-only, unpublished) `integration/v0.8.0` head**:
`git merge-base --is-ancestor e5f093e integration/v0.8.0` exits 0 (same for `3ae8a6b`), the introducing
merge being `e929a9b` ("Merge branch 'wip/neo/frontmatter-lossy-shapes' into integration/v0.8.0") at
2026-09-17T02:44:47Z — i.e. before this run claimed the card (02:47:34Z) and before this lane chose its
base (02:54:18Z). Neither is an ancestor of `origin/main` or of
`origin/wip/tech-head/mutation-fts-sync` (`8f2f362`), which is this branch's own ancestry. So the
demotion-durability fix — which changes what a *fresh open* serves for the card's arm-2 sequence — is not
on this branch's base, but it is not out of reach either: `integration/v0.8.0` carries it today. This lane
is based on the parent lane's tip `wip/neo/kept-rows-settle` @ `fd55cc2` (itself off the survey lane
`8f2f362`), because the card names **that** lane's instrument (`scripts/kept-rows-settle-probe.mjs`) as
the re-runnable probe and this change extends it. PR #38, base `wip/neo/kept-rows-settle`.

*Correction (2026-09-17, review round 1).* This section previously read "neither is an ancestor of
`origin/main`, `origin/wip/tech-head/mutation-fts-sync` (`8f2f362`) **or** the (local-only, unpublished)
`integration/v0.8.0` head", which is **false** for that last ref; the derived phrasing elsewhere in this
entry ("the unmerged demotion lanes") was wrong with it, and both are corrected in place — journals are
never regenerated here (`scripts/substrate-journal-sync.mjs` skips any task whose file already exists), so
the correction belongs in the entry itself rather than in a re-run.

## RED first (out of tree and in tree)

New **arm 4** of `scripts/kept-rows-settle-probe.mjs`: five cases, one per catch-up call site. Each seeds
the trigger's own carrier, appends **one foreign `claim_extracted` event** to the canonical log (the shape
a second process/host writer leaves behind), runs the trigger, then measures the store row, the lane rows,
recall in-process and recall after a fresh open of the same brain dir. The foreign event asserts a **NEW
fact** and is self-evidenced (`parent_ids: []`), so no fact match, no supersession and no other flow's
retraction logic can reach it: the only variable is the catch-up's lane effect.

Pre-fix (`PROBE_DIST` = the pre-fix build at `fd55cc2`; `reports/arm4-prefix-5cases.json`):

| trigger | served in-process | fresh open | row in store | row in lane |
|---|---|---|---|---|
| `forget` (decoy observation) | `[]` | `[claim]` | `active` | absent |
| `retention` (sweep, something expired) | `[]` | `[claim]` | `active` | absent |
| `quarantine` (review approve) | `[]` | `[claim]` | `active` | absent |
| `compile-foreign-scope` (compile scope A, event in B) | `[]` | `[claim]` | `active` | absent |
| `correct-foreign-scope` (correct in A, event in B) | `[]` | `[claim]` | `active` | absent |

Post-fix, all five serve equal and the lane carries the row (`reports/arm4-postfix-5cases.json`).

In-tree pin `test/protocol/replay-catchup-lane-sync.test.ts` (5 measurements, one per trigger): **RED at
`fd55cc2` with the final test content — 5 failed | 0 passed**, every failure
`expected [] to deeply equal ['claim_…']`, the store-row assertion passing (so the catch-up *did*
materialise the claim and the failure is the lane) → **GREEN 5/5** on the tip.

Two of the five cases are **not** in the card, and were found by enumerating the catch-up's callers
(`grep replayCatchUp src/`) rather than by taking the card's list: `compile()` re-syncs `options.scope` at
stage 4.5 while its catch-up replays the whole log tail, and `core.correct` re-syncs the *corrected*
claim's scope only. Both reproduce the defect on a foreign scope, measured above. The card's own note
that compile "is fine" is true only for the scope it targets.

## Decision (the card said decide, not take)

Two candidate shapes were on the table: make `replayCatchUp` report what it touched and re-sync at the
call sites (the card's first suggestion), or do the re-sync inside `replayCatchUp` when the caller passes
the lane (its second).

**Chosen: report.** Layer 1 owns no search lane and imports nothing from layer 3 today; passing a lane
*port* in would have meant either a layer-1 → layer-3 edge or a new interface that has to travel down
through exactly the same handler signatures that are already the plumbing problem. Instead the catch-up
returns `ReplayCatchUpResult { processed, touchedScopes }` and the layer that owns the lane decides what
to do about it:

- `src/layer1/replay.ts` — `replayCatchUp` collects `touchedScopes` through `processEvent` into
  `handleClaimExtracted` (the admitted claim's scope, added after `admitClaim` returns),
  `handleCorrection` (the target's scope, plus the merge target's scope on the `duplicate` branch, which
  can be a different scope) and `handleRetraction` (the claim target's scope; for an observation target,
  through `removeEvidenceFromClaims`).
- `src/layer1/corroboration.ts` — `removeEvidenceFromClaims` takes an optional `touchedScopes` out-param
  and adds the scope of every claim whose row it writes. No extra scan of the store.
- `src/layer3/search.ts` — new `resyncCatchUpScopes(searchIndex, store, touchedScopes)`: nothing when the
  lane is absent or nothing was touched.
- The five call sites pass the lane they own: `handleForget` (new optional trailing `searchIndex`),
  `handleExpireRetention` (`RetentionDeps.searchIndex`), `handleQuarantineReview` (new optional trailing
  `searchIndex`), `handleCorrect` (same), and `compiler.ts` (which re-syncs the touched scopes that stage
  4.5 does not already cover, i.e. everything except `options.scope`).
- `src/core.ts` (the four wrappers) and `src/index.ts` (the MCP `smartware_forget`,
  `smartware_quarantine_review`, `smartware_correct` tools) pass `this.searchIndex` / `searchIndex`.
  **Putting the repair in the handlers, not the wrappers, is the deliberate part**: `src/index.ts` is a
  second dispatcher surface that calls the handlers directly, so a wrapper-only fix would have left the
  MCP surface broken.

Why scoped rather than a whole-lane rebuild: the touched set is exact (collected from the writes
themselves), the cost is proportional to what the catch-up materialised, and a catch-up that replayed
nothing reports `[]` — the common single-writer case pays nothing. `SmartwareCore.open` keeps its global
re-sync; this is the same repair narrowed to the scopes that moved. The re-sync is semantics-neutral (it
makes the lane equal to whatever the store says is indexable), so it holds under either reading of the
demotion question the unmerged lanes own.

One adjacent correction: `handleQuarantineReview`'s catch-up was **not awaited**
(`replayCatchUp(evidenceDir, store, layer0, config);` as a bare statement). It is awaited now — the
re-sync must follow the replay, and un-awaited the catch-up's failures escaped the handler entirely.

## What the settled arms now say (before/after, same instrument)

Re-ran arms 1–3 on the pre-fix and post-fix builds and diffed the verdict blocks
(`reports/pre-arm{1,2,3}.json` vs `reports/post-arm{1,2,3}.json`):

- **arm 1 (the kept-row settle): identical.** `served_equal_in_process_vs_fresh`,
  `counter_delta_total_found` (+1), `counter_delta_filtered_out` (+1), `views_agree_after_rebuild` — all
  unchanged. The FORGET claim-target case replays its own audit tombstone, finds the claim already
  retracted, touches no scope and therefore re-syncs nothing: the settled decision stands untouched.
- **arm 3 (the demotion mechanism): identical** (all four fields), as expected — no catch-up involved.
- **arm 2 (the drain probe): changed in exactly one way.** `subject_served_in_process` went from `[]` to
  `[C2]` — the live lane now serves the replacement claim the catch-up admitted, which is the fix. Its
  `subject_served_equal` is still `false` on this base because the **fresh** leg is the demotion
  *resurrection* (`t_8779781f` arm 3: a replay-admission demotion whose canonical record says
  `state: active` is erased by the next open), not a catch-up gap: the demotion-durability fix covers that
  leg — `integration/v0.8.0` carries it today, this branch's base does not. `kept_row_counter_delta` moved
  from `0` to `-1` for the same reason (the touched scope's lane is
  now rebuilt, so live counters drop the stale kept row while the fresh open's resurrected C1 keeps
  it). Nothing here is attributable to a lane the fix should not have touched: the re-sync is scoped to
  the scope the catch-up wrote.

## Reachability (as the card asked for, stated plainly)

A run is affected only when the catch-up meets a **claim-materialising event this process has not
replayed**:

- a `claim_extracted` L0 event written by a host or another process — OBSERVE itself rejects
  pre-extracted claims (`observe.ts:135-137`), so this is the retained replay path for legacy and
  host-written logs;
- a `correction` event written by another writer (`core.correct` in this process re-syncs the corrected
  scope itself, so the win here is a foreign or foreign-scope correction);
- another writer's tombstone/redaction that moves claim rows.

A single-writer process replays its own events at `open`, so the live window needs a multi-process or
multi-host brain — or, inside one process, an event asserting into a scope the verb's wrapper does not
re-sync (the COMPILE and CORRECT cases above, which is why they are pinned rather than dismissed as
theoretical). Production frequency is **not** measured; the probe is n=1 per configuration.

## Evidence

```
branch   wip/tech-head/catchup-lane-sync (off wip/neo/kept-rows-settle @ fd55cc2); PR #38, base wip/neo/kept-rows-settle
RED      vitest run test/protocol/replay-catchup-lane-sync.test.ts @ fd55cc2 (detached worktree, final test content)
         → 5 failed | 0 passed, every failure `expected [] to deeply equal ['claim_…']`
RED      node scripts/kept-rows-settle-probe.mjs --arm 4 (PROBE_DIST = pre-fix build @ fd55cc2)
         → all five triggers: served_in_process [] vs served_fresh [claim]; lane_has_claim_row false
GREEN    same five cases on this lane's build → all_triggers_serve_equal true
GREEN    vitest run test/protocol/replay-catchup-lane-sync.test.ts → 5 passed (5)
Gate     npm run build                          → tsc, exit 0
         npm test                               → 79 files, 562/562 passed (157.9 s)
         npm run verify:schemas                 → Verified 31 schema files across v0.4.2, v0.5.0
         npm audit --omit=dev --audit-level=low → 0 vulnerabilities
         npm run verify:saas                    → SMOKE_OUTCOME=pass
         npm run verify:coffee-adapter          → 53/53 checks, ADAPTER_SMOKE_OUTCOME=pass
         npm run verify:coffee-gate             → 85/85 checks, GATE_RUNNER_OUTCOME=pass
                                                  artifact smartware-0.7.0.tgz sha256 dc5d6658…af4c7
         (build/test/gate re-run once after a comment-only `src/layer3/search.ts` fix so the quoted
          numbers belong to this revision; `audit`/`schemas`/`saas`/`adapter` were run on the same tree)
Evidence /opt/data/workspaces/brain-pilot-evidence/catchup-lane-sync-20260917T025418Z/
Run      2026-09-17, 2 vCPU host, Node v26.5.1, n=1 per configuration
Not run  npm ci (VERIFY.md row 1 — this worktree's node_modules is a symlink to the shared checkout, so
         row 1 is not runnable here; CI covers rows 1–5 on the Node 22/24 matrix) and the three retrieval
         benchmarks (no ranking change; they are the gate for kernel/arena/activation changes)
```

CI on this branch: read the head SHAs and their `check-runs` from PR #38 (`verify (22)` and `verify (24)`,
the two-line matrix) — the journal deliberately does not name a revision, since a follow-up commit moves
it. `docs/STATUS.md` was regenerated on the tip after the last commit.

## Resulting state

- The invariant the survey set — **after any claim-row mutation, in-process recall == what a fresh open of
  the same brain serves** — now also holds across a mid-session catch-up, on all five call sites and on
  both dispatcher surfaces (`SmartwareCore` and the MCP server).
- `replayCatchUp` reports what it touched (`ReplayCatchUpResult.touchedScopes`); layer 1 still owns no
  search lane. `resyncCatchUpScopes` is the single place that turns that report into lane work.
- New pin `test/protocol/replay-catchup-lane-sync.test.ts` (5 tests) and new probe arm 4 (5 cases);
  `docs/conformance-status.md` carries the verified baseline (562/79) and the lane narrative,
  `docs/retrieval.md` states the behaviour for integrators (host-side claim persistence in
  `docs/integration/saas-integration.md` §1d is unchanged — it is about host *store* inserts).
- Review round 1 (docs/text only, no `src/` change asked for or made): the false containment sentence and
  its derived phrasing are corrected under *Base and containment* and *Unresolved* above (plus the same
  phrase in `docs/conformance-status.md`, which this lane added), and the two headers that undercounted the
  call sites — this test file's and arm 4's in `scripts/kept-rows-settle-probe.mjs` — now read five (four
  verbs + the compile pipeline), matching the enumeration and the `resyncCatchUpScopes` comment. The
  pre-existing occurrences of that phrasing in `docs/journal/2026-09-17-t_8779781f.md` and the inherited
  `docs/conformance-status.md` passage are that other lane's text and were left alone.

## Unresolved

- **`src/index.ts`'s verb-level lane repairs are still missing** (read, not measured): `smartware_correct`
  calls `handleCorrect` and does no lane work of its own, so on the MCP surface a corrected fact answers
  `[]` until a restart — the `t_8ddfa350` defect on a second surface. Routed as a follow-up card with the
  exact call path; the fix belongs with that surface (the same reason this change was placed in the
  handlers).
- REVISE re-syncs nowhere on this base (PR #29 unmerged), so its MCP tool has the same gap; that is PR
  #29's surface question.
- The arm-2 live-vs-fresh difference on this base is the demotion resurrection (`e5f093e`/`3ae8a6b`),
  which is not on this branch's base. **Re-run trigger, already satisfied somewhere today:** whenever this
  stack lands on a demotion-durable tree — `integration/v0.8.0` is one today (`e929a9b`) — re-run
  `node scripts/kept-rows-settle-probe.mjs --arm 4` and `--arm 2` there: arm 2's meaning changes once
  demotion is durable, because its fresh leg stops resurrecting C1.
- Not measured: production frequency of the reachability window; independent verification of this lane
  (producer-run evidence — a VERIFY card or reviewer run is the binding verdict).
