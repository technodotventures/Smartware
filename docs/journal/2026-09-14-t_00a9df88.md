---
task: t_00a9df88
board: smartware
status: done
completed: 2026-09-14T16:56:02.000Z
created: 2026-09-13T15:19:39.000Z
assignee: "neo"
created_by: "default"
artifacts: ["/opt/data/workspaces/brain-pilot-evidence/gauntlet-postfix2-20260914T165142Z/README.md","/opt/data/workspaces/brain-pilot-evidence/gauntlet-postfix2-20260914T165142Z/results.json","<repo>/docs/adr/0006-export-restore-return-path.md"]
---

# P0 verify/fix: single-writer resilience, recovery and portability gauntlet

## Intent

Immediate product goal: a robust Smartware build testable by Coffee as a company brain across shared workspaces, human staff, and agents. Active implementation repo is <repo>. Do not touch the divergent hermes-dev checkout. The repositories were recreated clean after a malware incident: never restore old objects or copy payloads; read /opt/data/vps-agents/ops/security/malware-cleanup-containment.md before any repository recovery operation. Distinguish VERIFIED, DESIGNED, and NOT YET PROVEN. Parity means Coffee-relevant behavior, not cloning competitor shells.
Exercise the serialized implementation against a throwaway Coffee-shaped deployment: owner SIGKILL, TTL takeover, process crash during write, SQLite WAL recovery, lease loss during request, Redis outage, brain unavailable with honest degraded reads, clean shutdown/restart, index wipe/rebuild, backup/restore, upgrade and rollback with existing data. Verify no split brain, no acknowledged lost write, no invented provenance and bounded recovery times. Implement only defects exposed. Add import/restore equivalence if export exists without a tested return path. Produce measured p50/p95 and recovery evidence.

## Resulting state

Resilience gauntlet run twice (before/after) against a throwaway Coffee-shaped deployment, 16 drills. Three real defects fixed: (1) a Redis outage killed the pilot process via an uncaught TimeoutError from the client's offline command queue — now fails closed with an honest 503 and recovers in 2.76 s; (2) value-blind lease renewal (SET..XX) could take back a lease already handed to another replica

**Artifacts recorded:**

- `/opt/data/workspaces/brain-pilot-evidence/gauntlet-postfix2-20260914T165142Z/README.md`
- `/opt/data/workspaces/brain-pilot-evidence/gauntlet-postfix2-20260914T165142Z/results.json`
- `<repo>/docs/adr/0006-export-restore-return-path.md`

## Recorded notes

- **neo** (2026-09-14 16:55Z): ## Resilience gauntlet — measured, fixed, re-measured **Deployment under test:** throwaway Coffee-shaped pilot (Redis app store + one Smartware brain + two replicas, Redis lease). Two full runs of the same 16-drill suite: pre-fix (`gauntlet-prefix-20260914T164140Z`, build `533df05`) and post-fix (`gauntlet-postfix2-20260914T165142Z`, build `9cbe1fb`). Harness: `<data>/dev-workspaces/<ws>

## Provenance

- Canonical record: kanban board `smartware`, task `t_00a9df88` (`task_events` append-only log).
- Actors recorded: neo
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
