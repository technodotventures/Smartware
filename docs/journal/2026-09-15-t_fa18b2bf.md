---
task: t_fa18b2bf
board: smartware
status: done
completed: 2026-09-15T10:34:17.000Z
created: 2026-09-15T10:15:38.000Z
assignee: "tech-head"
created_by: "neo"
artifacts: ["<data>/dev-workspaces/<ws>/VERDICT.md","<data>/dev-workspaces/<ws>/gate.log","<data>/dev-workspaces/<ws>/results-A.json","<data>/dev-workspaces/<ws>/results-B.json","<data>/dev-workspaces/<ws>/results-C.json","<data>/dev-workspaces/<ws>/results-D.json","<data>/dev-workspaces/<ws>/results-D2.json","<data>/dev-workspaces/<ws>/results-mcp.json","<data>/dev-workspaces/<ws>/tools-diff.json","<data>/dev-workspaces/<ws>/rerun-adv.log"]
---

# VERIFY (independent, F1–F3 fix): hold-release keying + duty-scoped replay convergence + durable config writes + v0.5.0 ops enum completion (wip/neo/legal-hold-findings @ 29f5642, PR #12)

## Intent

INDEPENDENT VERIFY (VERIFY.md §Verification independence): the fix for the findings you raised on t_55fdccdd changes a **published-property surface** (v0.5.0 ops-log enum + SHA256SUMS) and **verification logic** (new pins), so the producer (neo) must not be its only verifier. Producer card: t_7a64ded2.
**Change under verification.** Branch `wip/neo/legal-hold-findings` @ `29f5642` (`b156bab` fix + `29f5642` docs), stacked on the verified-with-findings lane tip `wip/neo/legal-hold-marker` @ `7d13e2a`. PR #12 (base `wip/neo/legal-hold-marker`, OPEN, unmerged): https://github.com/technodotventures/Smartware/pull/12. Diff to read: `git diff 7d13e2a..wip/neo/legal-hold-findings -- src schemas test docs`.
**Producer's claims.**
1. F1 fixed: `hold.release` requires `operation_id` (core type + both MCP surfaces: `src/mcp.ts`, `src/index.ts`), refused `invalid_parameter` before any mutation; result always carries it; mcp_smoke pins the tool schema's `required`.
2. F2 fixed: `saveConfig` writes atomically + fsync (temp file → fsync → rename → directory fsync; `writePrivateFileDurable`, `writePrivateFile` unchanged for other callers), and a release replay **re-publishes** the recorded release into `config.holds` when that hold still reads OPEN. Convergence is **duty-scoped** via a new `hold_operation_id` in the receipt details: the hold's opening operation must match the recorded one, else nothing is written (fail-closed).
3. F3 fixed: v0.5.0 `operation-log-entry` enum gains `consolidate` / `reflect.explicit` / `retention.expire` (additive; `SHA256SUMS` regenerated to `f93051a0…`); R1 validates the sweep-skip receipt against the published schema.
4. Gate on the committed tree: `tsc` clean · **541/541 vitest across 75 files** · `verify:schemas` 31 OK · `verify:saas`

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_fa18b2bf`.)_

## Resulting state

VERDICT: verified with findings at wip/neo/legal-hold-findings @ 29f5642 (PR #12, OPEN/unmerged) — no producer claim falsified, artifact untouched (local == origin == 29f5642, nothing committed/pushed/merged, both verifier worktrees left clean). Gate reproduced from scratch in a fresh worktree (npm ci clean; tsc clean; 541/541 vitest across 75 files; verify:schemas 31 OK; verify:saas pass; 0 prod

**Artifacts recorded:**

- `<data>/dev-workspaces/<ws>/VERDICT.md`
- `<data>/dev-workspaces/<ws>/gate.log`
- `<data>/dev-workspaces/<ws>/results-A.json`
- `<data>/dev-workspaces/<ws>/results-B.json`
- `<data>/dev-workspaces/<ws>/results-C.json`
- `<data>/dev-workspaces/<ws>/results-D.json`
- `<data>/dev-workspaces/<ws>/results-D2.json`
- `<data>/dev-workspaces/<ws>/results-mcp.json`
- `<data>/dev-workspaces/<ws>/tools-diff.json`
- `<data>/dev-workspaces/<ws>/rerun-adv.log`

## Recorded notes

- **tech-head** (2026-09-15 10:33Z): ## VERDICT: verified with findings (no producer claim falsified; no gate failure) Artifact untouched: `wip/neo/legal-hold-findings` local == `origin` == `29f5642` (PR #12 still OPEN, base `wip/neo/legal-hold-marker`, head `29f5642`, mergeState CLEAN, **not merged**). No commit, no push, no tracked edit; every scratch step left `git status --porcelain` empty. Verify trees: `.worktrees/t_fa18b2bf` (
- **tech-head** (2026-09-15 10:34Z): Addendum (no verdict change): the harness runs were repeated once so every `results-*.json` artifact in `<data>/dev-workspaces/<ws>/` holds the **complete** final set (B = S6–S13, C = C1–C4). Timing-sensitive numbers in the re-run: `C2` 14 SIGKILL trials → `trials_that_reached_a_commit: 14`, `any_partial_destination: false` (first run: 12 after-commit / 2 mid-write, also 0 partia

## Provenance

- Canonical record: kanban board `smartware`, task `t_fa18b2bf` (`task_events` append-only log).
- Actors recorded: tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
