---
task: t_15bb0cd0
board: smartware
status: "accepted — decision recorded; F2 fixed on branches, F1 released (session delta; the task completes on the board this session)"
recorded: 2026-09-15T07:44:57Z
created: 2026-09-14T15:05:17Z
assignee: "neo"
created_by: "smarty-pants"
branch: "docs/protocol-identity-adr (off fix/duplicate-claim-recipe @ 93a7cfd, local, unpushed)"
artifacts: ["docs/adr/0005-protocol-claim-identity.md", "docs/adr/0003-claim-fact-identity.md", "docs/STATUS.md"]
---

# ADR-0005 accepted: protocol claim identity decided under founder delegation (F2 fixed, F1 released)

## Intent

Close `t_15bb0cd0` after the founder directive of 2026-09-15 removed the owner gate:
"decide this yourself, record in an ADR, implement if it needs code — no owner gate. The
ADR/PR is the review surface." The predecessor session (2026-09-14, entry
[2026-09-14-t_15bb0cd0.md](2026-09-14-t_15bb0cd0.md)) delivered the brief + ADR-0005 as
**Proposed** and escalated for sign-off; this entry supersedes that entry's pending status
and records the acceptance delta.

Out of scope, deliberately: merge, push, release, npm action; any edit to normative
spec/protocol text (F1 owns the §238 wording change); any `src/`, `test/` or `schemas/` change.

## What was done

- **ADR-0005 → Accepted (2026-09-15)** — decision text unchanged: Rule A is the one
  fact-identity predicate (`claim_type` excluded), Rule B is the autonomous-creation
  idempotency key, and the two relations cross in both directions.
- **D7 updated to reality.** F1 is authorized and released for implementation
  (`t_2996a3ab`, assignee `@neo`; its choice of variant, and the spec §238 wording, are taken
  in that change with its own gate run). F2 moved from "defect" to **fixed on branches**: the
  demotion is recorded in the demoted claim's own canonical version records
  (`superseded_by` + `superseded_at`) and derived by every materialisation
  (`wip/neo/demotion-durability` @ `e5f093e`, extended by
  `wip/smarty/demotion-handbuilt-records` @ `3ae8a6b`; both unmerged). One correction is
  recorded with it: the carrier is a **substrate-internal record field, not the canonical
  `supersedes` relation kind** — spec §6 admits epistemic edges only with
  `origin ∈ {reviewed, user}`, and a mechanical dedup carries no user warrant.
- **D5 and Consequences** scope "treat resolved as provisional" to trees without the F2 fix,
  and carry a state-at-acceptance bullet (branches, merge path, and this session's
  re-verification).
- **ADR-0003** pointers updated: no longer "owner sign-off pending" (status line, deciders
  note, *Known divergence* blockquote, and the owner-decision paragraph now record
  completion; the decision text is untouched).
- **`docs/STATUS.md` regenerated** — the `**Pending decision:** 0005` line is gone and the
  Decisions table shows ADR-0005 Accepted.

## Evidence

```
Re-verify: node step2-both-surfaces-experiment.mjs   (copy; pre-fix dist @ 93a7cfd)
           → normalized-identical to evidence/22: two active claims for one fact
             (fp_cdb1754080840ac1 / fp_9cfa41b7d2568916), Rule A → 2, recall 2 → 1 → 2,
             control claims_created=0            — evidence/32-step2-owner-rerun.txt
Re-verify: node step3-demotion-durability.mjs        (copy; pre-fix dist)
           → normalized-identical to evidence/23: canonical record has no
             status/superseded_by; compile-sync re-activates the loser; a replay loses the
             demotion; JSONL grep 'superseded' = false
                                                — evidence/33-step3-owner-rerun.txt
F2 check:  step2 re-pointed at the F2-fixed dist (e5f093e)
           → RECALL after an autonomous restatement = 1 (pre-fix: 2)
                                                — evidence/34-step2-fixed-dist-owner-rerun.txt
Gate:      npm run build                          → exit 0 (no dist/ diff; git status shows
                                                     only the two ADR files modified)
Gate:      npm test                               → 69 files, 493 passed (493), exit 0
                                                     (docs-only change: control, unchanged)
Gate:      npm run verify:schemas                 → Verified 31 schema files, exit 0
Gate:      npm run verify:saas                    → SMOKE_OUTCOME=pass, exit 0
Gate:      npm audit --omit=dev --audit-level=low → found 0 vulnerabilities, exit 0
Gate:      npm ci                                 → NOT RUN: no dependency change in this
                                                     branch (node_modules is the shared
                                                     checkout's, symlinked)
Raw:       <data>/kanban/boards/smartware/workspaces/t_15bb0cd0/rerun-2026-09-15/evidence/40-gates.txt
Run:       2026-09-15, Node v26.5.1, branch docs/protocol-identity-adr @ 93a7cfd + this change
Notes:     The harness re-runs were made on copies, so the archived evidence/22 and /23 were
           not clobbered. Cases 1–6 remain measured evidence, not regression pins; pinning
           them belongs to F1/F2, where the behaviour changes.
```

## Resulting state

- The protocol's identity relationship is **binding**: one fact-identity predicate, one
  creation key, a declared crossing, and the ADR (with its PR) is the review surface.
- F2 (demotion durability) is fixed on branches and pending merge; F1 (creation side) is
  released for implementation as `t_2996a3ab`.
- The steady-state both-surfaces falsifier still does not pass on a tree without both
  changes; that is now a merge-sequencing question, not a decision question.

## Unresolved

- Merge/push/release of the branch chain (`fix/duplicate-claim-recipe` → this branch, plus
  the F2 branches) remains an owner-side action; `main` is `enforce_admins`-protected.
- `docs/journal/INDEX.md` remains a stale projection on this branch (regenerating would also
  write 13 other tasks' entries — dry-run measured 2026-09-14, unchanged and out of scope).
  This entry lands as a hand-written task entry (same pattern as the 2026-09-14 one) because
  the board journal sync has not run against this branch.
- The steady-state falsification run (≈10 rounds) has still not been executed; it cannot pass
  before F1 lands.
- ADR template/parser mismatch (`docs/adr/README.md` vs `scripts/substrate-status.mjs`) still
  unfixed; ADR-0005 and ADR-0003 use the parseable `**Status:**` form.
