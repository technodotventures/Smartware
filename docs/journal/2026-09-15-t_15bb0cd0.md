---
task: t_15bb0cd0
board: smartware
status: done
completed: 2026-09-15T07:48:02.000Z
created: 2026-09-14T15:05:17.000Z
assignee: "neo"
created_by: "smarty-pants"
artifacts: ["<checkout>/smartware/.worktrees/t_15bb0cd0/docs/adr/0005-protocol-claim-identity.md","/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/32-step2-owner-rerun.txt","/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/33-step3-owner-rerun.txt","/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/34-step2-fixed-dist-owner-rerun.txt","/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/40-gates.txt","/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/41-status-regen.txt","/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/42-commit.txt"]
---

# Decide protocol identity: does claim fact identity include claim_type, or is the spec fingerprint the autonomous-path truth? (ADR-0003 vs spec §193)

## Intent

Owner-decision brief. Found by the reviewer on `t_29739781` (round 1); it is the reason that card is back in the implementer's lane for a wording fix. **No code change is being requested here** — what is unresolved is *protocol identity semantics*, which needs a recommendation plus owner sign-off before anyone widens or freezes a contract.
## The question
Smartware now has **two identity rules over the same `claims` rows**, and they disagree in both directions:
1. **Fact identity (new, ADR-0003)** — `ClaimStore.findActiveFactMatches`: `(subject_id, predicate, scope, normaliseValue(object), validity.to === null)`. Survivor = lexicographically smallest claim id. Governs the host write path (integration guide §1e).
2. **Claim fingerprint (existing, normative)** — `computeStructuredClaimFingerprint(subject_name, predicate, object, scope, claim_type)` (`src/layer1/fingerprint.ts:20`), consumed by `reflect.auto` idempotency at `src/protocol/reflect.ts:351` → `FingerprintIndex.activeByFingerprint` (`src/compile_queue/fingerprint.ts:139`) with the `findSemanticMatch` scan fallback at `src/protocol/reflect.ts:894`. Computed on every `insertClaim` (`src/layer1/store.ts:488`). Normative basis: spec v1.6.16 §193/§238 ("`fingerprint` = stable claim identity over the normalized assertion + `scope` + `claim_type`; the autonomous-creation idempotency key"), protocol v0.5.0:201.
Decide: **which rule is protocol truth for "these two rows are one fact", and does fact identity include `claim_type`?** Options: (a) add `claim_type` to fact identity (breaks the "one claim per fact across classifications" reading, and the pilot's fixtures leave `claim_type` unset → defaults `'finding'`); (b) keep fact identity scoped to the host write path and declare the fingerprint rule the

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_15bb0cd0`.)_

## Resulting state

Decision accepted and recorded per the owner directive (no owner gate; the ADR/PR is the review surface): ADR-0005 → Accepted at `df72d4a` (docs-only, local, unpushed) — fact identity is Rule A with `claim_type` excluded; the structured fingerprint is the autonomous-creation idempotency key; the crossing is declared and measured. D7 refreshed to reality: F2 recorded as fixed on branches (carrier c

**Artifacts recorded:**

- `<checkout>/smartware/.worktrees/t_15bb0cd0/docs/adr/0005-protocol-claim-identity.md`
- `/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/32-step2-owner-rerun.txt`
- `/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/33-step3-owner-rerun.txt`
- `/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/34-step2-fixed-dist-owner-rerun.txt`
- `/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/40-gates.txt`
- `/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/41-status-regen.txt`
- `/opt/data/kanban/boards/smartware/attachments/t_15bb0cd0/42-commit.txt`

## Recorded notes

- **tech-head** (2026-09-14 15:47Z): ## Decision brief ready for owner sign-off **Answer:** fact identity is Rule A (`subject_id, predicate, scope, normaliseValue(object), validity.to === null`) and `claim_type` is **not** part of it. Rule B (the structured fingerprint) is the autonomous-creation idempotency key spec §193/§238 already name it — never a second fact-identity rule. The two relations **cross** (each merges rows the other
- **tech-head** (2026-09-14 15:53Z): ### Review-lane note (run 48): escalating to the operator, not self-approving Run 46 ended with `review_requested`; the dispatcher has now spawned this run against the card **in the review lane**. This run will not approve it, for two reasons that are both about the card's own rule: 1. **The card requires owner sign-off** on protocol identity semantics ("before anyone widens or freezes a contract"
- **tech-head** (2026-09-14 15:54Z): ### Audit note: why this session did not force a second terminal transition (run 46) The runtime asked run 46 for a terminal call (`kanban_complete` / `kanban_block`). Recorded here rather than acted on, because acting would have been wrong: - Run 46 already ended with its terminal transition — `review_requested`, recorded 15:48 (event `review_requested`, run 46 `outcome: review_requested`, `ended
- **tech-head** (2026-09-14 15:55Z): ## Review-lane verdict (round 1, artifact lens): no correctable defect — escalating for the owner signature the card names Prior attempts: 0 `changes_requested` → round 1. Lens: read the ADR and the brief cold, then checked every citation and re-ran the measurements myself. **Nothing in the deliverable is wrong or missing, so this is not a `request_changes`; the only unmet requirement is the one n
- **neo** (2026-09-14 16:37Z): **F2 (demotion durability, case 7) — fixed on a branch; ADR-0005's F2 wording can move from "defect" to "closed with boundaries" when this lands.** Branch `wip/neo/demotion-durability` @ `42daa84` (base `fix/duplicate-claim-recipe` @ `93a7cfd`), kanban `t_01ef0ede`. The demotion is now recorded in the demoted claim's own **canonical version records** (`superseded_by`, `superseded_at`) and every ma
- **default** (2026-09-15 07:40Z): Owner directive (Stevie): decide this yourself, record in an ADR, implement if it needs code — no owner gate. The ADR/PR is the review surface.
- **default** (2026-09-15 07:40Z): UNBLOCK: Neo owns the decision; record in ADR + implement
- **neo** (2026-09-15 07:47Z): ## Decision ACCEPTED — recorded on `docs/protocol-identity-adr` @ `df72d4a` (docs-only, local, unpushed) Per the owner directive (2026-09-15: "decide this yourself, record in an ADR, implement if it needs code — no owner gate; the ADR/PR is the review surface"), the Head of Technology has decided and recorded: **The decision (unchanged from the brief):** fact identity is Rule A — `(subject_id, pre

## Provenance

- Canonical record: kanban board `smartware`, task `t_15bb0cd0` (`task_events` append-only log).
- Actors recorded: tech-head, neo, default
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
