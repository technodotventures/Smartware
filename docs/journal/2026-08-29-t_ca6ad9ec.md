---
task: t_ca6ad9ec
board: smartware
status: done
completed: 2026-08-29T14:56:15.000Z
created: 2026-08-29T11:11:32.000Z
assignee: "tech-head"
created_by: "user"
artifacts: ["<checkout>/smartware/.spike/compile-latency/README.md","<checkout>/smartware/.spike/compile-latency/report.json","<checkout>/smartware/.spike/compile-latency.mjs"]
---

# G0: spike — compile-latency axis (sync write <100ms, compile <=5s @50k claims)

## Intent

Measure sync-raw write p95 (<100ms target, no LLM on write path) and async compile p95 (<=5s @50k claims) for sync-raw + async-compile. Validate state-based freshness semantics: unverified / EXTRACTED / FAILED.

## Resulting state

G0 compile-latency spike complete: sync-raw write p95 = 1.868ms @50k obs (target <100ms, VALIDATED, no LLM on write path); async compile p95 = 9,468,298ms @50k claims (target <=5s, INVALIDATED — 1,893.7x over budget) with root cause pinned to O(N^2) per-claim fingerprint dedup (findByFingerprint/findSemanticMatch) plus per-claim fsync JSONL appends. State-based freshness validated at scale and und

**Artifacts recorded:**

- `<checkout>/smartware/.spike/compile-latency/README.md`
- `<checkout>/smartware/.spike/compile-latency/report.json`
- `<checkout>/smartware/.spike/compile-latency.mjs`

## Provenance

- Canonical record: kanban board `smartware`, task `t_ca6ad9ec` (`task_events` append-only log).
- Actors recorded: none recorded
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
