---
task: t_0e3989eb
board: smartware
status: done
completed: 2026-09-15T10:57:00.000Z
created: 2026-09-15T10:33:33.000Z
assignee: "neo"
created_by: "tech-head"
artifacts: ["<data>/dev-workspaces/<ws>/gate.log","<data>/dev-workspaces/<ws>/results-writer-surface.json","<data>/dev-workspaces/<ws>/results-boundary.json","<data>/dev-workspaces/<ws>/mutation-test.log","<data>/dev-workspaces/<ws>/README.md","<data>/dev-workspaces/<ws>/pr-body.md"]
---

# Advisory (from t_fa18b2bf verification): writer-side OpType admits 4 ops the published v0.5.0 enum rejects, and the "refused invalid_parameter (core and MCP)" wording is loose for a missing MCP field

## Intent

Found by independent verification card `t_fa18b2bf` on `wip/neo/legal-hold-findings` @ `29f5642` (F1–F3 fix, PR #12). Advisory: **nothing here fails the gate** (541/541, scheme checksums clean, live MCP round-trip clean) and both items are outside the runtime behaviour that card verified. Evidence: `<data>/dev-workspaces/<ws>/` (`attack-D.mjs`, `attack-D2.mjs`, `results-D*.json`).
**1. Writer-side `OpType` is not a subset of the published v0.5.0 op enum.**
`src/ops_log/types.ts` admits four values the published `schemas/v0.5.0/operation-log-entry.schema.json` enum rejects:
`recall`, `watch.subscribe`, `watch.event`, `guardian`.
- No writer site exists in `src/` for any of them today (no `op: '<x>'` literal, no `appendOpLogEntry`/`runCommit` descriptor, no watch module), so the F3 claim "every op the reference implementation writes now validates" is true as stated — this is a latent gap, not a false receipt.
- `src/ops_log/types.ts` is untouched by the F1–F3 fix branch, i.e. pre-existing.
- Measured: writer ops (13) all validate; `union_members_not_in_enum = ["recall","watch.subscribe","watch.event","guardian"]`, `enum_members_not_in_union = ["reflect","grant","revoke","quarantine_review","repair"]` (the enum side is fine — a superset is safe).
- Minimum outcome: make the writer surface unable to emit an op the published contract rejects — either add a test that pins `OpType ⊇` nothing / `OpType ⊆ enum` (a `schemas-v0.5.0` test that asserts each union member validates), or drop/relocate the four dead union members, or list them in the enum. Whichever is chosen, the asymmetry should stop being undetectable. Note `schemas/v0.5.0/SHA256SUMS` is a **shared hotspot** across PRs #7/#8/#12 (already flagged on `t_7a64ded2`) — if this needs an enum edit, coordinate the

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_0e3989eb`.)_

## Resulting state

Both t_fa18b2bf advisories closed on `wip/neo/ops-enum-symmetry` @ `265b00a` (PR #15, stacked on PR #12's head, unmerged): (F-2) the writer `OpType` now derives from an exported `OP_TYPES` runtime list, the four dead members `recall`/`watch.subscribe`/`watch.event`/`guardian` are removed (zero writer sites; rejected by every published enum), and `test/schemas-v0.5.0.test.ts` pins `OP_TYPES ⊆` the

**Artifacts recorded:**

- `<data>/dev-workspaces/<ws>/gate.log`
- `<data>/dev-workspaces/<ws>/results-writer-surface.json`
- `<data>/dev-workspaces/<ws>/results-boundary.json`
- `<data>/dev-workspaces/<ws>/mutation-test.log`
- `<data>/dev-workspaces/<ws>/README.md`
- `<data>/dev-workspaces/<ws>/pr-body.md`

## Recorded notes

- **tech-head** (2026-09-15 11:14Z): Independent verification t_2b8776f7 is complete: **verified with findings** on `wip/neo/ops-enum-symmetry` @ `265b00a` (PR #15, OPEN) — every claim of yours reproduced, none falsified. Full report + evidence: `/opt/data/kanban/boards/smartware/attachments/t_2b8776f7/` (`README.md` is the index). Measured on the committed tree: 542/542 vitest across 75 files · `tsc` clean · `verify:schemas` 31 OK ·

## Provenance

- Canonical record: kanban board `smartware`, task `t_0e3989eb` (`task_events` append-only log).
- Actors recorded: tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
