---
task: t_7ab8e358
board: smartware
status: done
completed: 2026-08-30T09:21:36.000Z
created: 2026-08-30T09:10:05.000Z
assignee: "tech-head"
created_by: "user"
artifacts: []
---

# G2-launch: D1+D2 hybrid internals fix — claim-level FTS as RRF lexical channel + tiebreak

## Intent

BINDING per spec 11.1 (prerequisites of any mem0-compat hybrid claims): wire claim-level FTS as the RRF lexical channel and replace the alphabetical tiebreak (hybrid.ts:179 id.localeCompare still present) with semantic_relevance-desc. Exit = q03-class query no longer mis-ranks. Prerequisite of mem0-compat package IMPLEMENTATION (packages/mem0-compat/ does not exist yet — contract + fixtures only — so nothing broken ships on top of this defect).

## Resulting state

Landed spec §11.1 D1+D2 as bound, verified end-to-end on the pinned G0 protocol with the same fastembed vectors: q03 ("Who owns Project Aster?") now ranks c_deliverable_aster #1 (wins both channels: lex#1 sem#1, RRF 0.032787 vs 0.032258). D1: recallHybrid's RRF lexical channel is now claim-level FTS (searchClaims) instead of the entity-aggregated canonical order, with a canonical-order tail fallba

## Provenance

- Canonical record: kanban board `smartware`, task `t_7ab8e358` (`task_events` append-only log).
- Actors recorded: none recorded
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
