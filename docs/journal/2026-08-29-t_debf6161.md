---
task: t_debf6161
board: smartware
status: done
completed: 2026-08-29T15:26:48.000Z
created: 2026-08-29T11:11:45.000Z
assignee: "tech-head"
created_by: "user"
artifacts: []
---

# G2: observation FTS index (raw-searchable window)

## Intent

v0.5.0-bound. searchObservations is a full JSONL scan + substring match today (core.ts:665); raw-searchable freshness promise needs an observations FTS index (existing claims-FTS + Layer0Index patterns). Must support the state-based freshness window (unverified/EXTRACTED/FAILED).

## Resulting state

Built the v0.5.0-bound observation FTS index (spec §10a): SearchIndex now owns an observation_search_index FTS5 table with a state-based freshness column (unverified/EXTRACTED/FAILED — never time-based), plus indexObservation/removeObservation/removeObservationsByScope/updateObservationFreshness/updateObservationStatus/searchObservations/count/clear. Added syncObservationsFromEvidence (wipe-and-re

## Recorded notes

- **tech-head** (2026-08-29 15:26Z): hotspot: src/layer3/search.ts — sibling G2 run (t_f0a0702e, ops index) completed concurrently against this same dir workspace; ops-log files (src/ops_log/, src/protocol/reflect.ts) landed mid-run and were NOT touched by this task. Core/index/search files are mine; ops-index files are the sibling's. Workspace verified quiescent via md5 snapshots (45s) before final runs; no live writers remain (only

## Provenance

- Canonical record: kanban board `smartware`, task `t_debf6161` (`task_events` append-only log).
- Actors recorded: tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
