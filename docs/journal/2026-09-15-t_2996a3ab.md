---
task: t_2996a3ab
board: smartware
status: done
completed: 2026-09-15T08:17:31.000Z
created: 2026-09-14T15:42:43.000Z
assignee: "smarty-pants"
created_by: "tech-head"
artifacts: ["/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/70-verifier-step2-PREFIX.txt","/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/71-verifier-step2-POSTFIX.txt","/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/72-status-live-projection.md","/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/74-probe-output.txt","/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/73-probe-entity-invariance.mjs","/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/SHA256SUMS-verifier.txt"]
---

# Smartware identity F1: reflect.auto must consult fact identity before creating a bounded claim (kills the both-surfaces duplicate)

## Intent

## Context
Owner decision `t_15bb0cd0` (brief + ADR-0005 on branch `docs/protocol-identity-adr`, off `fix/duplicate-claim-recipe` @ `93a7cfd`) decided the protocol identity split: **Rule A** (`ClaimStore.findActiveFactMatches`, `src/layer1/store.ts:765`) is the one fact-identity predicate and `claim_type` is not part of it; the **structured fingerprint** (`computeStructuredClaimFingerprint`, `src/layer1/fingerprint.ts:20`) is the autonomous-creation idempotency key and must not be read as evidence about facts. **ADR-0005 D7 records this card as follow-up F1** — it was deliberately not done inside the decision card because it changes `reflect.auto` and a normative spec sentence, so it needs its own sign-off and its own gate run.
## The measured gap this closes (VERIFIED — reproduce it first)
Harness (public surface, one store): `/opt/data/kanban/boards/smartware/workspaces/t_15bb0cd0/step2-both-surfaces-experiment.mjs`, output `evidence/22-bothsurfaces-experiment.txt`.
- Host path writes a claim for fact F with `claim_type` **unset** → `ClaimStore` default `'finding'` (`src/layer1/store.ts:400`).
- `OBSERVE` a body whose deterministic extraction yields the **same assertion** (`Deadline: 2026-09-01.` → `deadline_is`), then `REFLECT`/`compile` → `reflect.auto` defaults `claim_type` to `'hypothesis'` (`src/protocol/reflect.ts:351`) → fingerprints differ → **a second active claim is created** (`claims_created: 1`), same `subject_id`, distinct stamped fingerprints `fp_cdb17540…` / `fp_9cfa41b7…`.
- Real recall surface: **2 claims for one fact**. After `resolveFactMatches`: 1 (and the demotion is not durable — see sibling defect card).
- **Control:** the identical shape with the host's `claim_type` = `'hypothesis'` produces `claims_created: 0` and corroboration attached —

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_2996a3ab`.)_

## Resulting state

Reviewed and APPROVED (round 1, artifact lens). Independently re-derived at `40a3632` without touching the artifact worktree: RED 2 failed/2 passed on pre-fix src → GREEN 4 passed on the fixed src; full suite 497/497 across 70 files (my first run's 4 MCP failures were my own missing dist, reproduced away); verify:schemas 31 OK; verify:saas 12 PASS + SMOKE_OUTCOME=pass; audit 0. End-to-end with the

**Artifacts recorded:**

- `/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/70-verifier-step2-PREFIX.txt`
- `/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/71-verifier-step2-POSTFIX.txt`
- `/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/72-status-live-projection.md`
- `/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/74-probe-output.txt`
- `/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/73-probe-entity-invariance.mjs`
- `/opt/data/kanban/boards/smartware/attachments/t_2996a3ab/SHA256SUMS-verifier.txt`

## Recorded notes

- **neo** (2026-09-15 07:47Z): **Gate lifted — F1 is authorized; the "own sign-off" in this card's body is satisfied by the accepted ADR-0005.** Owner directive (2026-09-15, on `t_15bb0cd0`): "decide this yourself, record in an ADR, implement if it needs code — no owner gate." ADR-0005 is now **Accepted** (`docs/protocol-identity-adr` @ `df72d4a`); its D7 records this card as F1, authorized and released. So: choose the implemen
- **neo** (2026-09-15 08:09Z): **Implementation evidence inventory — F1 (branch `wip/neo/f1-reflect-auto-identity`, commits `8841647` → `4323f56` → `40a3632`).** Durable copy of every transcript plus the harness copy used: `<data>/dev-workspaces/<ws>/t_2996a3ab-f1/` — bundle `evidence-bundle-t_2996a3ab.tgz` (sha256 `4cb693a3380b3790478c25eac6172021a2b1c33cefbbba044a1342daccf4ee66`), per-file checksums in `SHA
- **neo** (2026-09-15 08:11Z): **Implementation-run close-out note (run 68, neo).** This session attempted `kanban_complete` to close out the implementation run and the kernel refused it — `could not complete t_2996a3ab (unknown id or already terminal)` — because the card is claimed by the **active review run 70** (reviewer `smarty-pants`, heartbeating since 08:09). That refusal is the correct protection: the pending terminal t
- **smarty-pants** (2026-09-15 08:17Z): **Review run 70 (round 1, artifact lens) — APPROVED.** Independent verification of `wip/neo/f1-reflect-auto-identity` @ `40a3632` is complete; the artifact worktree was not modified by the reviewer (only the pre-existing untracked `node_modules` symlink). Two non-blocking observations recorded below; nothing in them is a defect of this change. **Re-derived, not re-read (verifier-side scratch workt

## Provenance

- Canonical record: kanban board `smartware`, task `t_2996a3ab` (`task_events` append-only log).
- Actors recorded: neo, smarty-pants
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
