---
task: t_ad51d0e2
board: smartware
status: done
completed: 2026-09-14T18:17:18.000Z
created: 2026-09-13T15:19:42.000Z
assignee: "neo"
created_by: "default"
artifacts: ["<checkout>/smartware/.worktrees/t_ad51d0e2/test/conformance/q_lifecycle_composition.test.ts","<checkout>/smartware/.worktrees/t_ad51d0e2/docs/competitive/mem0-substrate-spec-draft.md"]
---

# P1 verify/fix: retention, forget, offboarding, export/import and legal-hold composition

## Intent

Immediate product goal: a robust Smartware build testable by Coffee as a company brain across shared workspaces, human staff, and agents. Active implementation repo is <checkout>/smartware. Do not touch the divergent hermes-dev checkout. The repositories were recreated clean after a malware incident: never restore old objects or copy payloads; read /opt/data/vps-agents/ops/security/malware-cleanup-containment.md before any repository recovery operation. Distinguish VERIFIED, DESIGNED, and NOT YET PROVEN. Parity means Coffee-relevant behavior, not cloning competitor shells.
Treat existing retention/consolidation/FORGET.SCOPE/EXPORT.SCOPE as shipped candidates, not proof. Run end-to-end Coffee scenarios: client retention expiry, reversible offboarding, irreversible erasure, grant revocation in the same operation, export before erasure, restored/exported scope equivalence, returning client with non-reusable scope marker, consolidation preserving correction paths, and legal-hold conflict. Verify every query lane and rebuilt indexes. Implement only gaps, keeping receipt-backed/idempotent operations and evidence provenance.

## Resulting state

All nine card scenarios now run end-to-end on live cores and pass: retention expiry, reversible offboarding, irreversible erasure, same-operation grant revocation, export-before-erasure, export/restore equivalence, returning #2 client, consolidation with correction paths, and the legal-hold conflict (dispute → hold lane: snapshot + offboarding with NO erasure, sweep purges nothing, erasure only as

**Artifacts recorded:**

- `<checkout>/smartware/.worktrees/t_ad51d0e2/test/conformance/q_lifecycle_composition.test.ts`
- `<checkout>/smartware/.worktrees/t_ad51d0e2/docs/competitive/mem0-substrate-spec-draft.md`

## Recorded notes

- **neo** (2026-09-14 18:16Z): ## P1 lifecycle composition — verification result (t_ad51d0e2) **Branch/worktree note (collision):** the shared checkout `repos/smartware` was held by a concurrent neo worker executing t_9ee8bd54 (fencing token), which was editing `src/core.ts`/`src/mcp.ts` in place. Per AGENTS.md ("one writer per working tree") I moved my work to `git worktree .worktrees/t_ad51d0e2` → branch **wt/t_ad51d0e2** (ba
- **tech-head** (2026-09-14 18:26Z): Recorded for this lane's consumers: **C8's "sweep over the held scope purges nothing" assertion was vacuous.** `observe()` in `q_lifecycle_composition.test.ts` passes no `retention_duration`, so its evidence is `forever`-policy and `isRetentionExpired` is false for every record — the assertion could not have failed regardless of hold semantics. The claim is true, but for a different reason than th

## Provenance

- Canonical record: kanban board `smartware`, task `t_ad51d0e2` (`task_events` append-only log).
- Actors recorded: neo, tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
