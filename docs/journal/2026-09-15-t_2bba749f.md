---
task: t_2bba749f
board: smartware
status: done
completed: 2026-09-15T08:17:42.000Z
created: 2026-09-15T08:01:05.000Z
assignee: "tech-head"
created_by: "neo"
artifacts: ["/opt/data/kanban/boards/smartware/attachments/t_2bba749f/review-evidence-compact_1.txt"]
---

# Schema accuracy: tombstone-frontmatter snapshot block does not enumerate the demotion/release record fields

## Intent

## Why
`schemas/v0.5.0/tombstone-frontmatter.schema.json` → `snapshot` enumerates claim fields with
`additionalProperties: false`, and it does not include the demotion/release fields the claim record
envelope now carries: `superseded_by`, `superseded_at` (shipped 2026-09-14, `t_01ef0ede` /
`t_742e31f9`) and `superseded_by_origin`, `reinstated_by` (shipped 2026-09-15, `t_1db21462`). The
block's own description promises the snapshot holds "every field the claim schema requires" — so a
tombstone whose snapshot is a demoted claim would fail the published schema.
Today no in-tree writer puts those fields into a tombstone snapshot (the only snapshot writer is
`src/layer1/tombstone-backfill.ts`, building one from a legacy row; the compiler creates the
`wiki/tombstones/` category but writes no pages), so it is **latent** — a schema/contract accuracy
gap, not a live failure. Flagged during `t_1db21462` and deliberately left out of that PR's
approved blast radius.
## Task
- Add the four fields to the `snapshot` block in
`schemas/v0.5.0/tombstone-frontmatter.schema.json` (additive optional, mirroring

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_2bba749f`.)_

## Resulting state

Reviewed and APPROVED (round 1, artifact lens) at wip/smarty/tombstone-snapshot-envelope @ fbce04d, reproduced on a fresh reviewer worktree. The four envelope fields in the tombstone snapshot block are JSON.stringify-identical to claim.schema.json (definitions and descriptions), snapshot.required and additionalProperties are untouched, SHA256SUMS verifies by two independent paths (31 files OK), an

**Artifacts recorded:**

- `/opt/data/kanban/boards/smartware/attachments/t_2bba749f/review-evidence-compact_1.txt`

## Recorded notes

- **smarty-pants** (2026-09-15 08:10Z): **Handoff note for the reviewer (tech-head)** — details behind the metadata fields. **Shipped** (`wip/smarty/tombstone-snapshot-envelope` @ `fbce04d`, PR #8, 5 files, +127/−4): 1. `schemas/v0.5.0/tombstone-frontmatter.schema.json` — `snapshot` gains `superseded_by`,    `superseded_at`, `superseded_by_origin`, `reinstated_by` (additive optional, `deepEqual` to    `claim.schema.json`'s definitions, 
- **smarty-pants** (2026-09-15 08:10Z): **Terminal-state note (no self-approval) — implementer run 69 / profile `smarty-pants`.** This card is *not* missing a terminal transition from the implementer run. Reading the event log directly: - `run_id 69` (profile `smarty-pants`, this session) ended with `outcome: review_requested`,   `status: review` — that is its terminal action. My session env pins `HERMES_KANBAN_RUN_ID=69`;   run 69 is c
- **tech-head** (2026-09-15 08:17Z): **Review verdict: APPROVED — run 71 (`tech-head`), round 1 (artifact lens). No implementation files touched by the reviewer.** Independently reproduced on a **fresh reviewer worktree** at `fbce04d1e996c5d30c7447a6d550e4dac7fcf63b` (not the implementer's tree), Node v26.5.1 / npm 11.17.0: | Check | Result | |---|---| | `sha256sum -c schemas/v0.5.0/SHA256SUMS` | all OK; `tombstone-frontmatter.schema

## Provenance

- Canonical record: kanban board `smartware`, task `t_2bba749f` (`task_events` append-only log).
- Actors recorded: smarty-pants, tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
