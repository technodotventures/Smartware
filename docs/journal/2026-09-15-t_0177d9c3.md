---
task: t_0177d9c3
board: smartware
status: done
completed: 2026-09-15T10:48:09.000Z
created: 2026-09-15T10:06:47.000Z
assignee: "tech-head"
created_by: "tech-head"
artifacts: ["/opt/data/rev-evidence-t0177d9c3/review-evidence-t_0177d9c3.txt"]
---

# Retention sweep writer: handleExpireRetention's OperationId fallback is op_+sha256 hex (67 chars), which the published OperationId pattern rejects

## Intent

## Why
Reviewer finding on `t_85817375` (L1 record writer, legacy OperationId). Same class as
`t_2bba749f`/`t_9e124fe6`/`t_85817375` — a library writer emits a value the published contract
rejects — but a **different writer**, so it was outside that card's scope guard ("One writer
(`src/layer1/store.ts`) + its test; no protocol or FORGET-path change"). Measured during the
`t_85817375` review; not fixed there.
`src/protocol/retention.ts:211`, inside `handleExpireRetention` for each sole-evidence claim:
```ts
operation_id: params.operation_id ?? `op_${computePayloadHash({ sweep: 'expire', claim: claim.id, seq })}`,
```
`computePayloadHash` is `createHash('sha256').digest('hex')` (`src/layer0/idempotency.ts:25-28`), so the
fallback is `op_` + 64 lowercase hex = **67 chars**. `schemas/v0.5.0/claim.schema.json` requires
`operation_id` (`$ref` `common.schema.json#/$defs/OperationId`), pattern `^op_[0-9A-HJKMNP-TV-Z]{26}$`
(Crockford base32, no I/L/O/U) and states in its own description that "the required field set applies
to all versions (active and forgotten)" — so this value is rejected by the contract the library

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_0177d9c3`.)_

## Resulting state

Reviewed and APPROVED (round 1, artifact lens) at wip/smarty/retention-op-id @ 37c914c (base 0a68482). I read the diff cold first and then re-derived every claim myself: RED in a fresh worktree of 0a68482 with the new test copied in gives 2 failed | 2 passed failing exactly at the published-pattern assertion on the 67-char sha256-hex id, GREEN at the tip is 4/4 (12/12 with the sibling retention fi

**Artifacts recorded:**

- `/opt/data/rev-evidence-t0177d9c3/review-evidence-t_0177d9c3.txt`

## Recorded notes

- **smarty-pants** (2026-09-15 10:33Z): Terminal-state note for this run (implementation phase closed at 10:33 UTC). Lifecycle call was `kanban_request_review` (reviewer `tech-head`, run 107 → outcome `review_requested`); board now reads **status `review`**, `current_run_id null`. I am deliberately **not** calling `kanban_complete` on my own card: `VERIFY.md` ("Verification independence") requires a second agent for a change that touche
- **smarty-pants** (2026-09-15 10:34Z): Implementer run 107 closed at 10:33 via `kanban_request_review` (reviewer `tech-head`); the dispatcher then claimed the card as run 109 for the reviewer. A later automated prompt told this session the card was "still running" and asked for a `kanban_complete`; that call was **refused** by the kernel ("could not complete t_0177d9c3 (unknown id or already terminal)") because run 107 had already reac

## Provenance

- Canonical record: kanban board `smartware`, task `t_0177d9c3` (`task_events` append-only log).
- Actors recorded: smarty-pants
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
