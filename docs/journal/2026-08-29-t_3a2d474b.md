---
task: t_3a2d474b
board: smartware
status: done
completed: 2026-08-29T18:37:06.000Z
created: 2026-08-29T18:07:26.000Z
assignee: "tech-head"
created_by: "tech-head"
artifacts: ["<checkout>/smartware/src/protocol/export_scope.ts","<checkout>/smartware/test/protocol/export-scope.test.ts"]
---

# G3.1: smartware_export_scope — owner-only scope-bounded export tool + tests

## Intent

Implement the substrate export surface designed in spec §10c.4 (v0.11 NORMATIVE, docs/competitive/mem0-substrate-spec-draft.md) and docs/competitive/coffee-client-scope-flows.md F1. Coffee's "export everything about Acme" runs on this; without it F1/F3 default lanes are impossible.
CONTRACT (from §10c.4 — read the spec section and the flows doc BEFORE coding):
- Owner-only MCP tool `smartware_export_scope { actor_id, scope, operation_id? }` → result { export_id (exp_<ulid>), path, counts, manifest }. requireOwner like smartware_forget_scope (src/mcp.ts:395 pattern, auth/middleware.js requireOwner).
- ONE-boundary rule: every content record has scope = target; the only cross-scope records are (a) ops entries in the operation closure and (b) forget.scope audit markers for the target (they live in the POD scope by design). Manifest asserts scope_exclusive: true + per-source counts.
- Package layout under `<data_dir>/exports/<export_id>/`: observations.jsonl (all observations with scope=target, ANY status, canonical shape from src/layer0/types.ts Observation — use the raw JSONL records; scope field at Observation.scope), claims.jsonl (EVERY claim version record incl. forgotten/tombstoned; canonical ClaimVersionRecord shape from src/layer1/jsonl.ts iterAllClaimVersions — filter by record scope), evidence.jsonl (evidence items referenced by those claims' supporting_evidence id closure — evidence lives in the same evidence/ log as observations; verify record shape and where supporting_evidence IDs resolve), operations.jsonl (every op entry whose operation_id is referenced by the above records + forget.scope entries with details.scope === target; src/ops_log readAllOpLogEntries), entities.jsonl (entity rows WHERE scope = target, marked non-canonical — entity identity is a

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_3a2d474b`.)_

## Resulting state

G3.1 SHIPPED: implemented owner-only smartware_export_scope (spec §10c.4) — one-scope boundary canonical package (observations/claims/evidence/operations/entities .jsonl + manifest.json with per-file sha256 + aggregate, scope_exclusive assertion) under <data_dir>/exports/<export_id>/, deterministic idempotent export_id per operation_id, erased-scope exports return the empty package + deletion cert

**Artifacts recorded:**

- `<checkout>/smartware/src/protocol/export_scope.ts`
- `<checkout>/smartware/test/protocol/export-scope.test.ts`

## Provenance

- Canonical record: kanban board `smartware`, task `t_3a2d474b` (`task_events` append-only log).
- Actors recorded: none recorded
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
