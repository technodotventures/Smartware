---
task: t_7a64ded2
board: smartware
status: done
completed: 2026-09-15T10:16:38.000Z
created: 2026-09-15T09:27:39.000Z
assignee: "neo"
created_by: "tech-head"
artifacts: ["<data>/dev-workspaces/<ws>","<data>/dev-workspaces/<ws>","<data>/dev-workspaces/<ws>","<data>/dev-workspaces/<ws>"]
---

# legal-hold findings (advisory): release replay must converge config (F2) · require operation_id on smartware_hold_release (F1) · ops-log enum drift covers the new skip receipt (F3)

## Intent

Follow-up from independent verification t_55fdccdd (verdict: verified with findings). Nothing here failed the gate — these are the three actionable findings from adversarial testing of `wip/neo/legal-hold-marker` @ `7d13e2a` (base `wt/t_c5c999ba` @ `76c4aa9`). Reproduce each from a fresh worktree; the verifier's scratch test files are the minimal repros (read-only reference, do not commit them):
- `<data>/dev-workspaces/<ws>/test/conformance/zz-verify-adversarial.test.ts` (C3, C5)
- `<data>/dev-workspaces/<ws>/test/conformance/zz-verify-adversarial2.test.ts` (E3, E4, E5)
- logs: `<data>/dev-workspaces/<ws>`, `<data>/dev-workspaces/<ws>`
**F2 (primary — crash asymmetry, fail-closed but a false "released").** `saveConfig` → `writePrivateFile` is a plain non-atomic, non-fsync `writeFileSync` (`src/storage/private-fs.ts:28`), so a power loss can leave the `hold.release` ops entry durable while the config release is lost. `handleHoldRelease`'s replay path (`src/protocol/hold_release.ts:94-115`) then returns the recorded receipt *without* re-applying it to config: the log says released, `isScopeHeld()` is still true, erasure still refuses (`legal_hold_open` — good), but a same-`operation_id` retry silently reports success and never lifts the hold; only a *new* `operation_id` converges. Repro = E3: release with `op X`, reset `config.holds[scope]` to open, retry with `op X` ⇒ receipt returned + `isScopeHeld` still true + erasure refused. Fix direction: on the replay path, converge config when the hold is still open for that scope (and/or make the config write atomic+fsync like the intent writer).
**F1 (audit gap).** A release without `operation_id` performs the act and writes **no** ops entry (C3: `OPS_ENTRIES_ADDED_WITHOUT_OPERATION_ID= 0`; E5). The house convention is the

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_7a64ded2`.)_

## Resulting state

Fixed all three actionable findings from the t_55fdccdd verification on wip/neo/legal-hold-findings @ 29f5642 (PR #12, stacked on wip/neo/legal-hold-marker, OPEN/unmerged): hold.release now requires operation_id (core + both MCP surfaces, refused before mutation); a release replay converges config to the recorded release duty-scoped — and saveConfig is now atomic+fsync so the divergence window is

**Artifacts recorded:**

- `<data>/dev-workspaces/<ws>`
- `<data>/dev-workspaces/<ws>`
- `<data>/dev-workspaces/<ws>`
- `<data>/dev-workspaces/<ws>`

## Recorded notes

- **neo** (2026-09-15 10:16Z): hotspot: schemas/v0.5.0/SHA256SUMS — three open PRs regenerate this file line-by-line (#7 claim.schema.json + revise-request.schema.json, #8 tombstone-frontmatter.schema.json, #12 operation-log-entry.schema.json). Line-disjoint today, but the v0.5.0 schema set must be regenerated as a whole at merge time: each checksum line must match the *merged* file bytes, and `npm run verify:schemas` is the ga
- **smarty-pants** (2026-09-15 10:23Z): Cross-card consistency note from **t_0177d9c3** (retention sweep writer, smarty-pants) for your F1 on `smartware_hold_release`. **What I settled, and why it bears on your F1.** `src/protocol/retention.ts`'s forgotten-version fallback was `op_${computePayloadHash(...)}` — 67 chars of sha256 hex, rejected by the published `$defs/OperationId` pattern. Decision (card option 1, implemented, branch `wip

## Provenance

- Canonical record: kanban board `smartware`, task `t_7a64ded2` (`task_events` append-only log).
- Actors recorded: neo, smarty-pants
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
