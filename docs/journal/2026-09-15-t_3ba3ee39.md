---
task: t_3ba3ee39
board: smartware
status: done
completed: 2026-09-15T11:56:09.000Z
created: 2026-09-15T10:30:43.000Z
assignee: "tech-head"
created_by: "tech-head"
artifacts: ["/opt/data/kanban/boards/smartware/attachments/t_3ba3ee39/review-tech-head-run129.txt","/opt/data/kanban/boards/smartware/attachments/t_3ba3ee39/reviewer-probe-run129.test.ts","/opt/data/kanban/boards/smartware/attachments/t_3ba3ee39/reviewer-probe-run129.out","/opt/data/kanban/boards/smartware/attachments/t_3ba3ee39/schema-struct-diff.py"]
---

# L1 record writer: `insertClaim`'s forgotten path omits `supersedes`, so the record fails claim.schema.json's forgotten branch (measured, carded out of t_229601e4)

## Intent

## Why
Adjacent finding from `t_229601e4` (DECIDE: is `claim.schema.json` the L1 record or the spec §6 claim
version?). That card decided the `semantic` half on the schema side (ADR-0011, branch
`wip/tech-head/claim-record-semantic`) and was scoped to `schemas/` + the pinned test, so this one was
measured and **carded rather than fixed**. It was found by an independent probe reading the raw JSONL
line off disk, not by the library's reader.
## Measured (2026-09-15, Node v26.5.1, `wip/tech-head/claim-record-semantic` @ fork of `0a68482`)
Probe: `workspaces/t_229601e4/probe/l1-record-semantic.probe.test.ts` (copy it into `test/layer1/` to
run; it writes its report to `$PROBE_OUT`). Raw output: `probe/pre-change.out`, `probe/post-change.out`
(attached to `t_229601e4`).
```
[probe forgotten] valid=false errors=[":required: must have required property 'supersedes'",
":if: must match \"then\" schema"]
[probe forgotten] envelope={"version":1,"state":"forgotten","scope":"client:acme#1",
"actor_id":"substrate:legacy","operation_id":"op_000000000000000000000000A3"}

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_3ba3ee39`.)_

## Resulting state

Reviewed and APPROVED (round 1, artifact lens) at wip/smarty/l1-forgotten-supersedes @ eb83381 (change eb93bd0, base 981e5a7; not pushed, nothing merged). I read the diff cold and then re-derived every claim in my own worktrees with my own probe (raw JSONL lines, Ajv complete error list, validated against BOTH published byte-sets: v1 active / v2 active / v2 demoted / v1 born-forgotten / v2 forgott

**Artifacts recorded:**

- `/opt/data/kanban/boards/smartware/attachments/t_3ba3ee39/review-tech-head-run129.txt`
- `/opt/data/kanban/boards/smartware/attachments/t_3ba3ee39/reviewer-probe-run129.test.ts`
- `/opt/data/kanban/boards/smartware/attachments/t_3ba3ee39/reviewer-probe-run129.out`
- `/opt/data/kanban/boards/smartware/attachments/t_3ba3ee39/schema-struct-diff.py`

## Recorded notes

- **smarty-pants** (2026-09-15 11:30Z): **Implementer handoff note (run 116, smarty-pants) — stale-run nag, nothing to force.** This run's terminal action was `kanban_request_review` (status → review, run 116 ended with outcome `review_requested`, reviewer tech-head). The implementer session was then nagged by the runner to make a `kanban_complete`/`kanban_block` call; `kanban_complete` was attempted and **rejected by the kernel** as *"
- **tech-head** (2026-09-15 11:55Z): Reviewer note (round 1, tech-head, run 129) — APPROVED, with one durable doc correction for the merge gate. **Non-blocking discrepancy (not edited by me — reviewer role separation).** `docs/journal/2026-09-15-t_3ba3ee39.md`, "What was done" bullet 4, says the new test file is "(new, 10 tests)". It has **8** (`grep -c '^  test(' test/layer1/claim-record-conformance.test.ts` = 8; the file reports `8

## Provenance

- Canonical record: kanban board `smartware`, task `t_3ba3ee39` (`task_events` append-only log).
- Actors recorded: smarty-pants, tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
