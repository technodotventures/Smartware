---
task: t_037255f6
board: smartware
status: done
completed: 2026-09-15T15:32:24.000Z
created: 2026-09-15T11:52:42.000Z
assignee: "tech-head"
created_by: "neo"
artifacts: ["/opt/data/kanban/boards/smartware/attachments/t_037255f6/README.md","/opt/data/kanban/boards/smartware/attachments/t_037255f6/probe.py","/opt/data/kanban/boards/smartware/attachments/t_037255f6/probe-pins-and-placement.log","/opt/data/kanban/boards/smartware/attachments/t_037255f6/probe-tsc-pass.log","/opt/data/kanban/boards/smartware/attachments/t_037255f6/independent-probe-matrix.log","/opt/data/kanban/boards/smartware/attachments/t_037255f6/independent-gate.log","/opt/data/kanban/boards/smartware/attachments/t_037255f6/independent-gate-rerun.log","/opt/data/kanban/boards/smartware/attachments/t_037255f6/npm-build-on-drift.log"]
---

# VERIFY (independent): compile-time OpType derivation guard — wip/neo/optype-derivation-guard @ 3822f4b (PR #18, F-1 of t_2b8776f7)

## Intent

INDEPENDENT VERIFY (VERIFY.md §Verification independence): this change hardens the **verification logic** around a published schema surface (the writer `OpType` ⇄ v0.5.0 `op` enum invariant), so the producer (neo) must not be its only verifier. Producer card: t_9f0f314c. Origin: finding F-1 from the t_2b8776f7 verification of PR #15.
**Change under verification.** Branch `wip/neo/optype-derivation-guard` @ `3822f4b` (`e64fa62` guard + `3822f4b` docs), stacked on `wip/neo/ops-enum-symmetry` @ `265b00a` (PR #15's head). PR #18 (base `wip/neo/ops-enum-symmetry`, OPEN, unmerged): https://github.com/technodotventures/Smartware/pull/18. Diff to read: `git diff 265b00a..wip/neo/optype-derivation-guard -- src test schemas VERIFY.md docs`.
**Producer's claims.**
1. `src/ops_log/types.ts` carries a compile-time proof that `OpType` has no member outside `OP_TYPES` (`const _opTypesIsExhaustive: OpType extends (typeof OP_TYPES)[number] ? true : never = true;`). The card's exact repro — union widened with `| 'watch.subscribe'`, plus a real `CommitDescriptor` writer site (no cast) — now fails `tsc` with `TS2322` at the guard, where at `265b00a` it was tsc-clean (exit 0) with the pin green (8/8).
2. The proof must live in `src/` (not `test/`) because `tsconfig.json` excludes `test/` and vitest does not typecheck; CI's `npm run build` compiles it before the tests run. Re-inlining the union with a foreign member also fails the guard; adding a member to `OP_TYPES` leaves tsc green but still fails the existing pin (`1 failed | 7 passed`).
3. No schema file touched; `schemas/v0.5.0/SHA256SUMS` blob `dd70e667377aa5337fa18a2e3ea97d060ca9a2f5` identical at `265b00a` and `e64fa62`. `git diff --name-only 265b00a..3822f4b` = `src/ops_log/types.ts`, `VERIFY.md`, `docs/STATUS.md`.
4. Gate on the

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_037255f6`.)_

## Resulting state

Independently verified `wip/neo/optype-derivation-guard` @ `3822f4b` (PR #18, OPEN/unmerged) from a fresh detached worktree with my own `npm ci` and my own mutation harness. VERDICT: verified with findings. Every producer claim reproduced: the card's exact repro inverts (clean tsc exit 0; drift -> `npm run build` exit 2 TS2322 at types.ts(61,7) with a real CommitDescriptor writer site, while the s

**Artifacts recorded:**

- `/opt/data/kanban/boards/smartware/attachments/t_037255f6/README.md`
- `/opt/data/kanban/boards/smartware/attachments/t_037255f6/probe.py`
- `/opt/data/kanban/boards/smartware/attachments/t_037255f6/probe-pins-and-placement.log`
- `/opt/data/kanban/boards/smartware/attachments/t_037255f6/probe-tsc-pass.log`
- `/opt/data/kanban/boards/smartware/attachments/t_037255f6/independent-probe-matrix.log`
- `/opt/data/kanban/boards/smartware/attachments/t_037255f6/independent-gate.log`
- `/opt/data/kanban/boards/smartware/attachments/t_037255f6/independent-gate-rerun.log`
- `/opt/data/kanban/boards/smartware/attachments/t_037255f6/npm-build-on-drift.log`

## Recorded notes

- **tech-head** (2026-09-15 15:28Z): VERDICT: **verified with findings**. Fresh detached worktree at `3822f4b` + my own `npm ci` (Node 26.5.1 / npm 11.17.0, 2 vCPU); my own mutation harness; nothing committed, nothing pushed (`git ls-remote` head still `3822f4b`, both scratch worktrees clean). **Artifact identity.** origin `wip/neo/optype-derivation-guard` = `3822f4b` (= the card's revision). PR #18: OPEN, base `wip/neo/ops-enum-symm

## Provenance

- Canonical record: kanban board `smartware`, task `t_037255f6` (`task_events` append-only log).
- Actors recorded: tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
