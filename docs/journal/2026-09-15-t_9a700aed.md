---
task: t_9a700aed
board: smartware
status: done
completed: 2026-09-15T11:40:00Z
created: 2026-09-15T10:57:00Z
assignee: "neo"
artifacts: []
---

# Decision: host-registered lanes are not v0.5.0 scopes; the substrate has one ActorId (ADR-0015)

## Intent

Close the divergence carded out of `t_229601e4` (ADR-0011 → *Known divergences*, item 2): L1 records
`reflect.auto` appends through the pod-profile surface carried `scope: pod/<pod>/<lane>` and
`actor_id: substrate:<ULID>`, and the published v0.5.0 `Scope`/`ActorId` patterns reject both. The two
fields have different causes: the ActorId is the library's own identity (a writer defect), the scope is
the host's lane vocabulary (a protocol-surface question).

**Out of scope:** the Pod product's own code and data (a scope migration would be Pod-repo work); any
edit to normative protocol text (AGENTS.md rule 7); the `insertClaim` forgotten-path and
`semantic` divergences (other cards).

## What was done

- **ActorId — fixed in the writers, no protocol change.** One canonical helper
  `substrateActorId(config)` (`src/config.ts`) mints `substrate:<slug>`: `smartware_` prefix stripped,
  lowercased, sanitised (`smartware_coffee` → `substrate:coffee`, spec §5's example; an anonymous ULID
  instance → the lowercased ULID). `src/protocol/reflect.ts`, the compile queue
  (`src/compile_queue/worker.ts`, whose public `podActorId` now delegates) and `SmartwareCore.dream`
  all use it. Before: one instance minted **two** identities — `substrate:<UPPER ULID>` on claims and
  operations-log entries (pattern-rejected; Crockford base32 is case-insensitive, the published pattern
  is not) and `substrate:smartware-<ulid>` on dream entries.
- **Scope — decided and disclosed, not migrated and not widened.** v0.5.0's scope vocabulary stays
  closed. The `pod/<pod>/<lane>` ids are recorded as **host-registered lanes** — legitimate registry
  ids, not `Scope` values — in `schemas/v0.5.0/README.md` (new section), `docs/conformance-status.md`
  → *Remaining limits*, and the `createPodProfile` docstring. **ADR-0015** (Status: `Proposed`, owner
  approval is the merge gate) carries the decision, the measured evidence, the alternatives and the
  recommended destination (a host-lane form in the next protocol revision; the alternative is the Pod
  product migrating onto the vocabulary).
- **Tests.** New `test/layer1/pod-profile-conformance.test.ts` pins both halves on raw JSONL bytes:
  the complete Ajv error list on a pod-profile record is exactly `["/scope:pattern"]`, a
  protocol-native record's complete list is empty, reflect.auto and dream carry the same
  `substrateActorId`, and the slug rule is pinned for named/ULID instances.
  `test/schemas-v0.5.0.test.ts` gains the closed-vocabulary fixture (host-lane spellings rejected);
  `test/semantic-materialization.test.ts` no longer substitutes the actor id.
- **Adjacent measurements, carded, not absorbed.** The same probe validated the flow's other canonical
  artifacts: the raw L0 evidence line and the compiled wiki page frontmatter have a *different field
  shape* than `observation.schema.json` / `page-frontmatter.schema.json` — measured on the
  protocol-native control flow too, so it is **not** this divergence. Carded
  (`t_0920aa1d`: measure whether those schemas describe the writer's artifacts). The host-lane
  protocol fork is carded for the owner (see Unresolved).

## Evidence

```
Gate:   npm run build                        → tsc, exit 0
Gate:   npm test                             → 537 passed | 0 failed (74 files), exit 0
Gate:   npm run verify:schemas               → Verified 31 schema files across v0.4.2, v0.5.0
Gate:   npm run verify:saas                  → SMOKE_OUTCOME=pass
Gate:   npm run status; npm run status:check → regenerated; see Notes for the final check
Probe:  probe/pod-profile-record.probe.test.ts (independent: raw JSONL/markdown + Ajv)
        before: pod claim → ["/scope:pattern","/actor_id:pattern"]; reflect.auto ops → ["/actor_id:pattern"];
                dream ops → [] but a DIFFERENT id; protocol-native control claim → ["/actor_id:pattern"]
        after:  pod claim → ["/scope:pattern"] (documented host lane); reflect.auto AND dream ops → [];
                protocol-native control claim → []
Mutants: reflect.ts reverted to the old mint → 4 tests fail (3 new + semantic-materialization);
        common.schema.json Scope widened to admit host lanes → the new closed-vocabulary fixture fails
        (1 failed | 9 passed). Both restored; tree clean at the committed revision.
Run:    2026-09-15, container, 2 vCPU (load avg ~20 from other lanes), Node v26.5.1, n=1
Notes:  `npm ci` was run once in this worktree (fresh). `npm audit` not re-run (no dependency change).
        `docs/journal/INDEX.md` deliberately not regenerated (journal:sync would import other lanes'
        entries into this branch). Schema bytes unchanged: `sha256sum schemas/v0.5.0/common.schema.json`
        = 7949e3d1…, identical to `SHA256SUMS`.
```

## Resulting state

- One instance mints one substrate identity, and a test holds it: claims and operations-log entries
  written by reflect.auto, the compile queue and dream all carry the same conformant
  `substrate:<slug>`.
- The pod-profile surface is honestly positioned: host lanes keep working, and the v0.5.0
  schema-conformance claim no longer implicitly covers records written in them (the contract's
  "schema validity on every canonical write" boundary is stated where integrators read it).
- ADR-0015 exists as a **Proposed** record; branch `wip/neo/host-lane-identity` (forked from
  `wip/tech-head/claim-record-semantic` @ `981e5a7`), stacked PR open, nothing merged.

## Unresolved

- Owner approval before merge (ADR-0015 `Proposed`); no published schema byte moved, so the gate is
  about the conformance-claim wording and the recommended protocol revision.
- **The owner's fork (carded):** define a host-lane form in the next protocol revision (recommended;
  keeps live products and the lane taxonomy, needs its own design: reserved namespaces, registry
  requirement, visibility/hierarchy semantics, erasure, conformance impact) **or** the Pod product
  migrates onto the protocol-native vocabulary (cheaper protocol text, dearest product change).
- **L0 line / compiled page frontmatter shape** vs `observation.schema.json` /
  `page-frontmatter.schema.json` — measured, not this divergence, carded as `t_0920aa1d` for a proper
  measurement and decision.
- Historical rows keep the pre-fix `substrate:<ULID>` spelling (append-only; not rewritten). The alias
  map remains the read-time resolution mechanism if a host wants it; this change does not append
  per-instance alias entries.
- **Verification.** VERIFY.md → *Verification independence* required a second agent's verdict before
  merge, and `t_8f2c028a` (`tech-head`) supplied one, revision-scoped to `9114fb7`: all three audited
  claims — writer identity re-derived, no schema byte moved, both test pins and mutation checks
  reproduced — **PASS**, with the tooling caveat that the handoff's 537/0 was not reproducible under
  load (F-2; see the correction below). Every commit after `9114fb7` is documentation-only. Merge
  still awaits the owner's approval of ADR-0015.

## Record corrections

*Correction pass `t_ad84d246` — documentation only: no `src/`, `test/`, `schemas/` or `package*` byte
moves (see the gate re-run below).*

- **Dead card id, fixed.** The *Adjacent measurements* bullet above cited `t_3a4c0f0a`, which exists
  on no board — a placeholder written before the measurement card existed (the children were created
  at `11:36:08Z`, 87 s after this entry's commit `9114fb7` at `11:34:41Z`). The kernel-assigned card
  is **`t_0920aa1d`** ("MEASURE+DECIDE: raw L0 evidence line and compiled page frontmatter have a
  different field shape …", parent `t_9a700aed`, assignee `smarty-pants`). Corrected in place here and
  in *Unresolved*. Found by the independent verification of this lane (`t_8f2c028a`, finding F-1) and
  re-resolved read-only against every board on this host (`hermes-dev`, `optimus-os`, `smartware`,
  `test-board`, `venture-factory`).
- **`docs/` sweep.** `grep -rno 't_[0-9a-z]\{8\}' docs/` resolves every other match to an existing
  card or to a snake_case word fragment (`set_confidence`, `t_invalidated`,
  `custom_fact_extraction_prompt`, `audit_observation_id`, `grant_01kxw9f2v5`, …). `t_3a4c0f0a` was
  the only dead card reference in the tree; it now survives in this correction note alone (as the
  thing being corrected, not as a pointer).

**Gate re-run for the correction commit** (2026-09-15 ~18:00–19:30Z; container, 2 vCPU, shared host at
load avg ~180–220 with swap full — ~30–50× normal test latencies):

```
Gate:   npm run build                          → tsc, exit 0
Gate:   npm run verify:schemas                 → Verified 31 schema files across v0.4.2, v0.5.0
Gate:   npm run verify:saas                    → SMOKE_OUTCOME=pass (exit 0)
Gate:   npm audit --omit=dev --audit-level=low → found 0 vulnerabilities (exit 0)
Gate:   sha256sum -c schemas/v0.5.0/SHA256SUMS → 16/16 OK (common.schema.json 7949e3d1…)
Gate:   npm run status; npm run status:check   → regenerated; back-to-back check exit 1, board-counter
        drift only (6317 → 6349 events within 4 min of regeneration; an earlier diff also carried
        another lane's branch tip — the expected counter race, not a content disagreement)
Gate:   npm test                               → NOT green on this host, timeout-dominated:
        run 1 (canonical `vitest run`): aborted at 63 min — 12/74 files reported, every one with
          30 s test-timeout failures across files unrelated to this lane (semantic-core,
          retention-expire, legacy-operation-id, compile_queue/worker, …);
        run 2 (`--maxWorkers=1 --fileParallelism=false`): aborted at 25 min, same class, 2/74;
        run 3 (focused, `--testTimeout=180000 --hookTimeout=120000`, same assertions):
          16 passed | 4 failed (20):
          test/layer1/pod-profile-conformance.test.ts   4 passed
          test/schemas-v0.5.0.test.ts                  10 passed
          test/semantic-materialization.test.ts         2 passed
          test/conformance/mcp_smoke.test.ts            4 failed — MCP SDK `-32001 Request timed out`
            (the file that also fails at BASE — `t_8f2c028a`'s adversarial control)
Run:    2026-09-15, container, 2 vCPU, Node v26.5.1, n=1 per gate
Notes:  F-2 reproduced harder than at verification time (load ~20–49 then, ~180–220 now): a
        full-suite-green number is not obtainable on this host. This commit is documentation-only, so
        the code under test is the tree verified at `9114fb7`; the lane's own surface is green in
        isolation (run 3) and the only red file is the one already red at base.
```

