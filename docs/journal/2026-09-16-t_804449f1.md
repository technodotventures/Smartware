---
task: t_804449f1
board: smartware
status: done
completed: 2026-09-16T14:10:59.000Z
created: 2026-09-15T11:36:17.000Z
assignee: "tech-head"
created_by: "neo"
artifacts: []
---

# DECIDE (owner): host-registered lanes in the next protocol revision, or hosts migrate onto the Scope vocabulary (ADR-0012 fork)

## Intent

## Why
ADR-0012 (kanban `t_9a700aed`, Status `Proposed`) decided that host-registered lanes
(`pod/<pod>/<lane>`) are outside the v0.5.0 `Scope` vocabulary, and stated the consequence: a brain
that writes canonical records in host lanes does not meet the v0.5.0 conformance boundary
(*"schema validity on every canonical write"*) until its lanes are protocol-native or a revision admits
a host-lane form. ADR-0012 records the recommended destination and the alternative. **This card is the
owner's fork made operational; it is blocked until the owner rules.**
Artifacts to read first: `docs/adr/0012-host-registered-lanes-and-the-substrate-actor-id.md` (the
brief), `schemas/v0.5.0/README.md` → *Scope vocabulary*, `docs/conformance-status.md` → *Remaining
limits*. Branch: `wip/neo/host-lane-identity` (PR #17, unmerged, owner-gated). Live surface measured
in the Pod checkout @ `52fd85c` (`src/pod/data-spaces.ts`, `src/routes/workspaces.ts`).
## Step 0 — owner decision (this is why the card is blocked)
Owner picks one of:
1. **Host-lane revision (recommended in ADR-0012).** The next protocol revision defines a
host-registered lane form. Then: design it as a revision-level change — Scope-grammar text

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_804449f1`.)_

## Resulting state

DECIDE resolved by the owner. Option 1 chosen for the destination; decisions 1+2 of ADR-0012 approved for merge.

## Recorded notes

- **tech-head** (2026-09-15 11:52Z): ## Decision input prepared — owner ruling still required (Step 0) This run produced the measured fork instead of starting Option 1 or 2 (card's rule). Nothing normative moved, no schema byte changed, no product surface touched. **Durable packet (survives scratch cleanup):** `/opt/data/kanban/boards/smartware/attachments/t_804449f1/DECISION-PACKET.md` plus its instruments and raw outputs in the sam
- **tech-head** (2026-09-15 11:52Z): hotspot: `docs/adr/` — ADR numbering has collided and the next revision ADR needs an allocation decision before any worker writes it. Measured across all branches: `0012` is now **triple**-allocated (`0012-forgotten-version-supersedes`, `0012-grant-granularity-at-the-forget-scope-boundary`, `0012-host-registered-lanes-and-the-substrate-actor-id`), and `0005`, `0008`, `0010`, `0011` are each double
- **optimus (owner ruling)** (2026-09-16 14:10Z): OWNER RULING on the ADR-0012 host-lane fork (rendered on Stevie's explicit delegation: "go and I'll leave ADR 0012 to you"). 1) ADR-0012 decisions 1 and 2 are APPROVED as written.    - Decision 1 (ActorId): approved as a defect fix. One canonical lowercase      `substrate:<slug>` per instance, minted by `substrateActorId`, used by reflect.auto      (sync + background compile) and dream. It moves n

## Provenance

- Canonical record: kanban board `smartware`, task `t_804449f1` (`task_events` append-only log).
- Actors recorded: tech-head, optimus (owner ruling)
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
