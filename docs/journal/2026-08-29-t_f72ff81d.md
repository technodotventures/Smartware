---
task: t_f72ff81d
board: smartware
status: done
completed: 2026-08-29T16:24:24.000Z
created: 2026-08-29T11:11:52.000Z
assignee: "tech-head"
created_by: "user"
artifacts: []
---

# G2: sync-raw + async-compile write path + durable queue + state-based freshness

## Intent

RESOLVED default: observations written synchronously to L0 (deterministic, sub-100ms, NO LLM on write path); claims compiled in background on a durable queue. Retrieval includes raw observations for a STATE-based window with unverified flag in RECALL payload (never time-based; FAILED stays searchable forever; EXTRACTED = claim ranks above, obs kept as evidence). Requires both indexes (obs FTS for raw-searchable, ops index for compile intent matching).

## Resulting state

Built the sync-raw + async-compile write path: durable SQLite compile queue (indices/compile.db), O(1) fingerprint→claim_id dedup index, batched claim/ops appends (one fsync per month/day), state-based freshness labels (unverified/EXTRACTED/FAILED) moved to a fast meta table, and a background worker with per-observation fault isolation — wired through SmartwareCore (observe enqueue, drainCompileQu

## Recorded notes

- **tech-head** (2026-08-29 16:24Z): hotspot: src/layer3/search.ts — observation FTS/meta-table section rebuilt by BOTH t_debf6161 (original obs FTS) and t_f72ff81d (performance redesign); final shape = FTS rows keep metadata columns, observation_meta side table (obs_id PK, fts_rowid, scope, freshness) for O(log n) deletes + µs freshness transitions. Design decision recorded in the file header — workers must read it before touching t

## Provenance

- Canonical record: kanban board `smartware`, task `t_f72ff81d` (`task_events` append-only log).
- Actors recorded: tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
