---
task: t_74faf12d
board: smartware
status: done
completed: 2026-09-16T14:30:00Z
created: 2026-09-16T14:05:00Z
assignee: "tech-head"
artifacts: []
---

# Integration: rebase the ADR-0013 lane onto the moved base `9fcfff7`, regenerate `STATUS.md`, refresh the `ADR-0012` → `0015` labels (PR #19)

## Intent

The ADR-0013 branch (`wip/smarty/canonical-schema-boundary`, PR #19) was reviewed and approved at
`793c2b6`, but its base had moved under it: `wip/neo/host-lane-identity` went from the fork point
`9114fb7` to `9fcfff7`, whose `bc23ed1` refiles **this lane's own earlier ADR `0012` → `0015`**. GitHub
reported `mergeable=false` / `mergeable_state=dirty`, and `git merge-tree` agreed on a single conflict:
`docs/STATUS.md`. This entry records the mechanical integration step — **no decision, no measured shape,
and no schema byte changed**.

**Out of scope:** any edit to ADR-0013's decision, its measurement, or its status (`Proposed` — the owner's
approval is the merge gate); `schemas/v0.5.0/*.schema.json` and `SHA256SUMS`; renumbering any ADR.

## What was done

- **Rebase, not merge** (`git rebase 9fcfff7` in the branch's own worktree,
  `/opt/data/kanban/boards/smartware/workspaces/t_0920aa1d/repo`, clean before and after). The branch is
  **unmerged**, so a rewind + replay is the integration `AGENTS.md`/the kanban card sanctions; the
  pre-rebase tip is kept reachable as the local tag `t_74faf12d-pre-rebase` (`793c2b6`). All five commits
  replayed; the branch's own content is byte-identical where the base did not touch it (ADR-0013 text,
  the journal entry's body, both boundary tests — verified by `sha256sum` of `git show <ref>:<file>`).
- **`docs/STATUS.md` conflicts were resolved by regeneration**, never by hand: on both conflicted replays
  (`b9a7e02`, `e9c385a`) the driver ran `npm run status` and re-added the file, then verified no conflict
  marker survived. Post-rebase the only `STATUS.md` changes are the three the generator makes by itself:
  `**Branch:** HEAD` → the branch name (the lane worktree is attached again), the lane's own row leaving
  the in-flight table (the generator deliberately excludes the *current* branch,
  `scripts/substrate-status.mjs:82`), and live board counters.
- **The base's rename was propagated, not re-broken.** `bc23ed1` rewrote this lane's references in
  `docs/conformance-status.md` and `schemas/v0.5.0/README.md`; the rebase kept our additions and the base's
  `ADR-0015` labels in both files, and no tracked file other than *this sentence* references the old path
  `adr/0012-host-registered-lanes-and-the-substrate-actor-id.md` any more (it does not exist at the tip).
  The earlier wording here said "no reference anywhere in the tree", which overclaimed by counting this
  entry's own mention of the retired path.
- **Three prose uses of the retired label were refreshed** (the card's step 3): `docs/adr/0013-…:19`
  ("the host-lane divergence ADR-0015 settled") and the two "the ADR-0015 lane recorded" notes in
  `docs/journal/2026-09-15-t_0920aa1d.md`. The fourth (the `docs/STATUS.md` ADR-table row) is regenerated.
- **One correction beyond the label refresh, disclosed here and in the record:** re-running the
  number-allocation scan the card asked for (every local and `origin` ref, 86 refs, tree-level) shows
  `0013` is **double-allocated**: `wip/tech-head/retention-sweep-audit` committed
  `docs/adr/0013-retention-sweep-commit-identity.md` at `2026-09-15T12:09Z` (kanban `t_543cb61a`) and is
  **local-only, unpushed** — which is why the round-2 note "no other branch holds it" was wrong when it was
  written. ADR-0013's **header numbering note only** is corrected in place, marked as a rebase-time
  correction; the decision, the measurement table, the alternatives and the status are untouched, and the
  number is **not** renumbered (that is `t_73ea9510`, with the rule/registry in flight as PR #20).

## Evidence

```
Rebase: git rebase 9fcfff7 (5 commits replayed)      → tip 793c2b6 → 7d89d5c, tree clean, no marker survived
        base at fork 9114fb7 → base tip 9fcfff7; conflict set was exactly [docs/STATUS.md], as measured
Gate:   npm run build (tsc)                          → exit 0
Gate:   npm test                                     → 76 files / 541 tests passed, exit 0 (72.38 s; load 3.64/2.59/2.15)
Gate:   npm run verify:schemas                       → Verified 31 schema files across v0.4.2, v0.5.0, exit 0
Gate:   npm run verify:saas                          → SMOKE_OUTCOME=pass, exit 0
Gate:   npx vitest run test/layer0/l0-record-wire-boundary.test.ts \
                       test/layer2/l2-page-frontmatter-boundary.test.ts
                                                     → 2 files / 4 tests passed, exit 0 (5.06 s)
Gate:   npm run status; npm run status:check          → regenerated; "STATUS.md is current.", exit 0
                                                    (VERIFY.md row 7's order: status first, then check)
Frozen: sha256sum schemas/v0.5.0/SHA256SUMS          → 8d47a4275739994b268d8c1e9a803196fd9c68e9997a47f5bb25f46b2d3cc420 (unchanged)
        aggregate sha256 over all 31 *.schema.json   → f3c4804680c6685670cb7705e0acd979cc1a038c57956fd38830646095aa3939 (unchanged)
        git diff --name-only 9fcfff7..HEAD -- schemas/ → schemas/v0.5.0/README.md only (no *.schema.json)
Run:    2026-09-16, container, Node v26.5.1, n=1, load average 3.6–3.8 (low for this host)
Tree:   the numbers above are gate run 1, taken on tree `7d89d5c` with this entry and the label
        corrections in the working tree (uncommitted). Gate run 2, on the committed tip `43a2e3a`
        with a clean tree, reproduced every count: 76 files / 541 tests passed, exit 0 (66.78 s),
        boundary 2 files / 4 tests passed, exit 0 (3.92 s), `verify:schemas` 31 files OK,
        `verify:saas` SMOKE_OUTCOME=pass. The authoritative gate is CI (`.github/workflows/ci.yml`,
        `verify` on Node 22 and 24), which ran **green** on the pushed tip.
Notes:  the full suite passed clean, including test/conformance/mcp_smoke.test.ts — the 10 s stdio-hook
        flake the earlier runs on this branch recorded under load 23–42 did not reproduce at load 3.6.
        Gates 1–2 of VERIFY.md (`npm ci`, `npm audit`) were NOT re-run: no dependency file changed in this
        task (package-lock.json sha256 d4f5560a… is identical to the pre-rebase tree) and the lane tree's
        node_modules was installed from that lockfile.
```

## Resulting state

- `wip/smarty/canonical-schema-boundary` is **based on the current base tip** (`9fcfff7`) and was pushed
  twice while this entry was written: `43a2e3a` (the rebase plus the label corrections) and `6ed7a93` (the
  journal line naming this entry's gate provenance) — both tips carry a green CI `verify` (Node 22 and 24).
  The branch head moves as review rounds land, so **PR #19 is the record for the head SHA, not this line**;
  the round-1 correction commit is the head on top of `6ed7a93`. No published schema byte, no `SHA256SUMS`
  line and no ADR number moved; `docs/STATUS.md` is regenerated, not hand-edited.
- ADR-0013 is still **`Proposed`** — this entry does not approve it, and the owner's merge gate is
  unaffected. The two carded fixes (`t_8d6f4a5c`, `t_f1157ed4`) can now fork from a clean tree.
- `docs/journal/INDEX.md` is **deliberately not regenerated** here, matching this lane's earlier rounds:
  `npm run journal:sync` imports every other lane's entries into a shared generated file, which would put
  unrelated lanes' churn in this change. `npm run status` reports 40 completed tasks without a journal-index
  row; that count is the standing backlog the sync is for, not something this rebase created.

## Review round 1 (independent, `neo`, artifact lens) — one required fix, applied

Verdict: *changes requested*, one required doc correction; everything else independently re-derived clean
(merge-tree `9fcfff7 × 6ed7a93` exit 0, API `mergeable=true`/`clean`, range-diff rebase fidelity, frozen
surface reproduced from blobs, `STATUS.md` regeneration, gate re-run in the reviewer's own worktree, and the
ADR-0013 numbering-note judgment call confirmed in scope and correctly stated).

The required fix was **this entry's own stale sentence**, which said the branch "pushes as `7d89d5c`" —
`7d89d5c` is the mid-rebase tip (and was never pushed: `check-runs` for it returns `total_count=0`); the
pushed tips were `43a2e3a` and `6ed7a93`. The clause was true when drafted on the `7d89d5c` tree and went
stale when the integration commits landed. Corrected above, and rewritten so it cannot re-stale: the entry
names the pushed tips as of writing and points at PR #19 for the current head. Two optional wording
nitpicks raised in the same review are also fixed (the "two the generator makes" count, and the
self-referential "no reference anywhere in the tree" claim, which counted this entry's own mention of the
retired path).

## Unresolved

- **`0013` is still double-allocated** across `wip/smarty/canonical-schema-boundary` (this lane) and
  `wip/tech-head/retention-sweep-audit` (`t_543cb61a`, unpushed). Under the merge-order rule the claim
  reaching `main` second refiles before merge; `t_73ea9510` owns the allocation question and PR #20 carries
  the registry + `verify:adrs` check. Both of those are **unmerged**, so the registry is not yet a fact this
  record can rely on.
- The owner's approval of ADR-0013 (D2 direction) is still open, and PR #19 must not be merged before it.
- `status:check` compares the committed projection against a live regeneration; it passed at
  2026-09-16T14:16Z. Sibling lanes move the board's event counter several times a minute, so a later run
  will report STALE on counter rows alone — the same residual every lane records.
