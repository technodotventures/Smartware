---
task: t_229601e4
board: smartware
status: done
completed: 2026-09-15T10:56:37.000Z
created: 2026-09-15T09:41:51.000Z
assignee: "neo"
created_by: "smarty-pants"
artifacts: ["/opt/data/kanban/boards/smartware/attachments/t_229601e4/review-evidence-t229601e4.txt"]
---

# DECIDE: is claim.schema.json the L1 record or the spec §6 claim version? An active record's internal `semantic` block fails the published schema

## Intent

## Why
Adjacent finding from `t_85817375` (L1 record writer, legacy OperationId) — measured there but
deliberately **not** fixed, because that card's ask was the `operation_id` placeholder and its scope
guard was one writer. This is the `semantic` half of the same measurement.
`insertClaim` → the L1 JSONL append writes an active record carrying a `semantic` materialization
block (`src/layer1/jsonl.ts` → `ClaimSemanticMaterialization`, commented *"Optional for backwards
compatibility with pre-v0.6 materializations"*). `schemas/v0.5.0/claim.schema.json` has root
`additionalProperties: false` and does not enumerate `semantic`, so the record the library writes is
rejected by the contract the library publishes.
## Measured (2026-09-15, Node v26.5.1, worktree of `t_85817375` @ `0a68482`)
Probe — byte-identical to the `t_9e124fe6` attachment `l1-record-placeholder.probe.test.ts` (copy kept
at `workspaces/t_85817375/probe/`, and in this task's attachments as `t_85817375-evidence-extras.tgz`):
store with `setDataDir` → `insertClaim` with `operation_id: null` → read the JSONL record →
Ajv-validate against `schemas/v0.5.0/claim.schema.json`:
```

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_229601e4`.)_

## Resulting state

Reviewed and APPROVED (round 1, artifact lens) at wip/tech-head/claim-record-semantic @ 981e5a7 (change b49ae4b, base 0a68482; not pushed). Independently re-derived the measurement in reviewer worktrees: probe RED @0a68482 `:additionalProperties(semantic)` -> GREEN @981e5a7 `errors=[]`; schema verified additive-only (required identical, 0 drifted properties, closed kept); SHA256SUMS OK (claim sha

**Artifacts recorded:**

- `/opt/data/kanban/boards/smartware/attachments/t_229601e4/review-evidence-t229601e4.txt`

## Recorded notes

- **tech-head** (2026-09-15 10:44Z): ## Decision + implementation (round 1, ready for independent review) **Decided: option 1** — `claim.schema.json` describes the **L1 claim version record** (one line of the canonical L1 JSONL, as the reference implementation appends it), so the record envelope is enumerated in it, including an **optional, closed** `semantic` block. Recorded as **ADR-0011** (`docs/adr/0011-claim-record-materializati
- **tech-head** (2026-09-15 10:46Z): Lifecycle note (for the operator, after a harness nudge asked for `kanban_complete`/`kanban_block`): This card is already in its terminal state for run 106 — `status: review`, assignee `neo`, run outcome `review_requested` at 1789469054, `completed_at`/`result` untouched — so no further lifecycle call is valid from the producer side, and the kernel agrees: - `kanban_complete(...)` → `could not com
- **neo** (2026-09-15 10:56Z): ## Independent verdict — APPROVED (round 1, artifact lens) Reviewed `wip/tech-head/claim-record-semantic` @ `981e5a7` (change `b49ae4b`, base `0a68482`) in reviewer-owned worktrees; the deliverable worktree was not touched (clean, only the untracked `node_modules` symlink). **Re-derived, not trusted:** probe RED @`0a68482` → `:additionalProperties(semantic)`; GREEN @`981e5a7` → `valid=true errors=

## Provenance

- Canonical record: kanban board `smartware`, task `t_229601e4` (`task_events` append-only log).
- Actors recorded: tech-head, neo
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
