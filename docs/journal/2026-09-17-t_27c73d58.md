---
task: t_27c73d58
board: smartware
status: done
completed: 2026-09-17T00:45:00Z
created: 2026-09-17T00:31:00Z
assignee: "tech-head"
artifacts: []
---

# An unscoped REFLECT compiles every scope, and the operations log records it as unscoped

> Hand-authored: `scripts/substrate-journal-sync.mjs:138` skips a task whose file already exists, so a
> later sync will not regenerate or correct this entry (only `INDEX.md` is regenerated). Fix an
> inaccuracy here in place and say so, rather than waiting for the generator.

> **Correction 2026-09-18 (review round 1 → run 255).** Three statements in this entry were wrong or
> imprecise and are corrected in place below, each marked where it stands: (1) the *Idempotency* bullet
> said a replay of an already-used unscoped `operation_id` "replays to its recorded result rather than
> recompiling" — measured, it **re-runs the compile** on this lane and only *reports* the recorded claim
> count; (2) the same sentence in *Unresolved*; (3) the *Intent* framing that the sentinel matched no
> registered lane — the filter's `endsWith('/' + scope)` clause also matched a host-registered lane
> ending in `/personal`, measured on a pod-profile brain (`core.createPodProfile`), which the round-1
> review flagged as an inference it had not run. All three were re-measured first-party here (my own
> worktrees and builds of `658c3cb` and `48cdc0b`, the round-1 probe used verbatim) rather than copied
> from the review. **No code, test, or schema byte moved:** the incremental diff from the reviewed
> revision `48cdc0b` to the correction commit is `docs/` only.

## Intent

Carded out of `t_e6fce49a` (the consent-change lane fix), where the adjacent literal was measured and
deliberately left alone: `src/protocol/reflect.ts` resolved a REFLECT with no scope to the lane
literal `personal` —

```ts
// Omitting scope compiles ALL scopes; only the owner may do that.
const targetScope = params.scope ?? 'personal';
```

— and that value is not a label. `reflectAutoCreateClaims` **filters** observations by it
(`if (scope && obs.scope !== scope && …)`; the full clause is
`!obs.scope.endsWith('/' + scope) && !obs.scope.startsWith(scope + '/')`), and both `reflect.explicit`
writers put it in the operations-log entry's `details.scope`. So a no-scope owner compile did not compile
all scopes: it compiled the lanes that filter selects for `personal` — the id `personal`, which no
Core-opened brain registers (registration has been `self` / `workspace` / `project:default` since the
initial commit), **plus any host-registered lane ending in `/personal` or starting with `personal/`**.
That second half is a measurement, not a reading: on a brain with a pod profile
(`core.createPodProfile('demo')`, which registers `pod/demo/personal` + `pod/demo/workspace`,
`src/core.ts:435`) the pre-fix unscoped owner compile produced **1 claim / 1 page, in
`pod/demo/personal`**, while the entry recorded `details.scope: "personal"` — a name that is neither the
lane actually compiled nor a lane registered on that brain. (Round-1 review flagged this reach as a
code-read inference it had not run; `attachments/t_27c73d58/pod-personal-with-id.log` is the run — see *Evidence*.)

The card asked for a **measurement and a decision** (what a no-scope owner compile should mean, and
what the ops entry should record for it), the fix, and an A/B pin — on a lane stacked on
`wip/neo/consent-change-scope` @ `658c3cb` (PR #24). **Out of scope, deliberately:** the two
consent-change writers and their two `?? 'personal'` fallbacks (fixed on `t_e6fce49a`), the L2
page-frontmatter lane (`t_8d6f4a5c`), everything on the ADR-0013 lane, widening `$defs/Scope`,
re-introducing the literal anywhere, and merging (requirement 2 of the card: the owner-gated class —
see *Resulting state*).

## What was done

### The decision

**A no-scope compile means: no scope filter — every lane.** That is not a new contract invented here;
it is the one the handler already states in its own header and the one every other reader of the
target already implements:

- `CompileParams.scope?: string` — optional by declaration.
- `CompileOptions.scope` → `src/layer2/compiler.ts:92` `if (options.scope && obs.scope !== options.scope) continue;`
  — absence gathers every observation.
- `reflectAutoCreateClaims`' filter (`reflect.ts:794` pre-fix) — the same `if (scope && …)` shape.
- `syncSearchFromClaims(store, searchIndex, scope?)` → `replaceClaimIndex(claims, scope?)`, and
  `store.getActiveClaims(scope?)` — both already treat absence as "no lane restriction".
- `handleCompile`'s own ownership gate exists *because* the unnamed form spans lanes: "only the owner
  may compile all scopes."

One line disagreed with all of that, and it was the line that decided both the claim-production filter
and the audit label. The fix deletes the sentinel; **the absence is the spelling**. No lane literal is
introduced in its place, so no sentinel needs naming: nothing in the tree consumes a lane name for the
unscoped case (measured — see *Evidence*; the only reader of a `reflect.explicit` entry keys on
`op` / `actor_id` / `details.payload_hash`, and nothing reads `details.scope` on one).

**The ops entry records `scope: null` for an unscoped run**, and the named lane otherwise. Rationale:
`payload_hash` in the same entry is computed over `scope: params.scope ?? null`, so `null` keeps the
entry self-consistent with the payload it is idempotency-keyed on (measured: the hash is byte-identical
pre/post fix for the same no-scope params); `null` is distinguishable by a machine reader from a key a
writer forgot to emit; and it is not a lane, so it cannot be mistaken for a registered scope.
`details` is free-form in `schemas/v0.5.0/operation-log-entry.schema.json`, so this is a semantics
choice, not a schema change — no schema byte moves.

### The change

`src/protocol/reflect.ts` only (+ the pin):

- `const targetScope = params.scope ?? 'personal';` → `const targetScope = params.scope;`
- `requireGrant(actor, 'compile', targetScope, config)` → `if (targetScope) requireGrant(…)` — an
  unnamed compile is owner-gated by the check two lines above, which is **unchanged**; a named lane is
  still grant-checked exactly as before.
- `reflectAutoCreateClaims`' `scope: string` parameter → `scope: string | undefined` (its filter
  already guarded on falsiness).
- Both `reflect.explicit` writers: `scope: targetScope` → `scope: targetScope ?? null`.
- `CompileOptions.scope: params.scope` → `targetScope` (one source of truth for the target).

No `$defs/Scope` widening, no `schemas/` byte, no `SHA256SUMS` line, and no literal re-introduced:
`grep -n "personal" src/protocol/reflect.ts` is empty after the change.

### Disclosure: this changes behaviour (the owner-gated class)

Stated explicitly, because the card requires the call to be visible before any merge:

- **Claim production for an unscoped owner compile goes 0 → all lanes.** Measured on a fresh brain
  with one extractable observation per registered lane: an unscoped owner `reflect()` produced
  **0 claims / 0 pages** pre-fix and **3 claims / 3 pages** (one per lane) post-fix — exactly the sum
  of the three per-lane runs, which is the oracle the pin asserts.
- **The deferred-synthesis path changes with it**: `syncSearchFromClaims(store, searchIndex,
  targetScope)` now indexes every lane's claims (measured: `layer3_indexed_count` 0 → 3, and a
  `recall` for the claim in each lane goes 0 → 1 hit). Pre-fix that path synced the `personal` lane.
- **Operations-log shape changes for unscoped runs only**: `details.scope` is `"personal"` pre-fix,
  `null` post-fix (`claims_created` / `pages_compiled` also move because the run now does work).
  Scoped runs are unchanged in shape and in content.
- **Unchanged, and pinned as controls on both arms**: a named lane still filters to that lane and is
  recorded by name; a non-owner without a scope is still refused `invalid_scope`; a non-owner with a
  granted lane still compiles exactly that lane; a non-owner on an ungranted lane still gets
  `insufficient_permission`.
- **Reachability**: only the owner can reach the changed path (the guard is unchanged and is the only
  gate); a host that always names a scope sees no behaviour change at all.
- **Idempotency** — *corrected 2026-09-18; this bullet previously said the opposite of what was measured.*
  `payload_hash` for the same params is unchanged (measured identical pre/post,
  `6a2fcd59…`), so an `operation_id` already used by an unscoped run is not refused as a payload
  conflict. But on this lane such a replay is **not** a no-op. Measured on the upgrade path — a brain
  written by the pre-fix build (`658c3cb`), then opened by the fix (`48cdc0b`) and given the **same**
  `operation_id`, with an attribution control (fix build opens the same brain and runs nothing: state
  unchanged) — the compile **re-runs**: claims 0 → 3 (one per lane), pages 1 → 5,
  `layer3_indexed_count` 0 → 3, six `reflect.auto` receipts, and **no** second `reflect.explicit` entry.
  The returned result is the mixed one: it reports the *recorded* `claims_created` 0 beside a *freshly
  measured* `pages_compiled` 3. Source at this revision agrees: `reflect.ts:501` raises `conflict` only
  on an `op` / `actor_id` / `payload_hash` mismatch and otherwise falls through into the compile;
  `reflect.ts:698` returns `compiled.pages.length` while `:699` returns
  `parentEntry.details['claims_created']`. A **same-revision** replay (this fix writes, this fix replays)
  *is* a no-op — claims stay 3, no entry is appended — so the non-idempotence is specific to an id
  recorded by a pre-fix build. The consequence for a host, stated plainly: reusing an old unscoped
  `operation_id` on an existing brain is **not** inert — it does create the new claims and pages; it just
  reports the old claim count while doing so, so the log and the result describe different amounts of
  work. **This is the replay-reporting defect the round-1 review measured and carded, not this lane's fix:**
  it is `t_efa8d5a8`, delivered on `wip/tech-head/reflect-replay-contract` (PR #32 — base is this branch,
  open and unmerged as read back on 2026-09-18 — ADR-0018), where a matched id becomes a genuine no-op
  returning both recorded counts. Under that lane, the sentence a host needs is the simple one; on *this*
  revision, read the paragraph above.
- **Migration**: none needed. L0/L1 history is untouched and append-only; entries already on disk keep
  the spelling they were written with (the ADR-0015 precedent, as on `t_e6fce49a`). Nothing gates on a
  `reflect.explicit` entry's `details.scope` (measured).

No ADR is filed from this lane: the decision is stated here and in the PR body, and freezing it in
`docs/adr/` would pre-empt the owner gate the card attaches to the merge (the ADR-0015 pattern —
*Status: Proposed — owner approval is the gate before merge* — is the form to use if the owner wants it
recorded as one after the call).

## Evidence

All commands run in this session, in worktrees of the shared repo (the working copy the sibling lanes
use was not touched). Evidence files are declared on the board (`attachments/t_27c73d58/`).

```
Gate:   npm run build                    → exit 0
Gate:   npm run verify:schemas           → "Verified 32 schema files across v0.4.2, v0.5.0, v0.5.1", exit 0
Gate:   npx vitest run (full suite)      → 78 files / 554 tests passed, exit 0 (69.3 s)
Gate:   npm run verify:saas              → SMOKE_OUTCOME=pass, exit 0
A/B:    test/protocol/reflect-no-scope-all-scopes.test.ts, sha256 51dc5162… on BOTH arms
          at 658c3cb (pre-fix, worktree t_27c73d58-red)  → 3 failed | 3 passed, exit 1
              the three failures are the three unscoped assertions:
                "an unscoped reflect must compile exactly the union of the lanes"  {} vs {self:1, workspace:1, 'project:default':1}
                "the entry must not name a lane the caller did not ask for"        'personal' !== null
                deferred synthesis: claims_created 0 !== 3
              the three controls (named lane, non-owner gate, non-owner grant) pass on both arms
          at 6d1c73b (fix)                              → 6 passed (6), exit 0
Run:    2026-09-17, container, Node v26.5.1, host load ~2 (n=1)
```

The measurement behind the decision, from the untracked probe (`attachments/t_27c73d58/
reflect-noscope.probe.test.ts` + `pre-fix-probe.json` / `post-fix-probe.json`), on a fresh
Core-opened brain seeded with one extractable body per registered lane:

```
                                                    pre-fix (658c3cb)        post-fix (6d1c73b)
registered lanes                                    self, workspace, project:default   (same)
owner reflect (no scope)   claims_created            0                        3
                           claims by scope           {}                       {self:1, workspace:1, 'project:default':1}
                           pages_compiled            0                        3
                           ops details.scope         "personal"               null
                           ops details.claims_created 0                       3
                           ops payload_hash          6a2fcd59…               6a2fcd59…   (identical)
owner reflect (scope=workspace)  claims_created      1                        1
                           ops details.scope         "workspace"              "workspace"
owner reflect (scope=self)       claims_created      1                        1
owner reflect (scope=project:default) claims_created 1                        1
owner compile (no scope, defer_synthesis)  claims_created 0                  3
                           layer3_indexed_count      0                        3
                           recall hits (each lane)   0                        1
non-owner reflect (no scope)                        invalid_scope            invalid_scope
non-owner reflect (scope=workspace, granted)        1 claim                  1 claim
non-owner reflect (scope=self, ungranted)           insufficient_permission   insufficient_permission
```

Reader sweep for the entry-shape half (`evidence/scope-readers.txt`): the only `details.scope` readers
in the tree are `reflect.ts:167` (`isReflectAutoTerminalReceipt`, gated on `entry.op === 'reflect.auto'`),
`ops_log/recovery.ts:666` and `ops_log/intent.ts:323/439` (observe / forget.scope intents). No reader
touches `details.scope` on a `reflect.explicit` entry, and the ops index stores `details` as one JSON
blob (`ops_entries.details TEXT`), so nothing downstream of the index parses the field either.

The two measurements added on 2026-09-18 (run 255, in answer to the round-1 review), both first-party —
my own worktrees, my own `tsc` builds of `658c3cb` and `48cdc0b`, fresh brain per arm:

```
Cross-revision replay (attachments/t_27c73d58/upgrade-path-firstparty.log; the round-1 probe
attachments/t_27c73d58/replay-probe.mjs used verbatim, sha256 b9da1e1f…; the same script on the
reviewer's builds gives an identical log — attachments/t_27c73d58/compare-firstparty-vs-reviewer.txt,
30/30 field comparisons equal):
  step 1  PRE-FIX build, fresh brain, unscoped REFLECT (id X)   claims 0 pages 1 ops_lines 1
          entry: scope "personal", claims_created 0, pages_compiled 0, payload_hash 6a2fcd59…
  step 2  FIX build opens that brain, runs NO reflect           unchanged (attribution control)
  step 3  FIX build replays the SAME id X                       result claims_created 0 / pages_compiled 3
          brain: claims 0→3, pages 1→5, ops_lines 1→7 (6 × reflect.auto + the original reflect.explicit,
          which still reads scope "personal" / claims_created 0); no second reflect.explicit entry
  control FIX build, fresh brain, run then same-revision replay  claims stay 3, no second entry
                                                                 (so the non-idempotence is the upgrade case)

Pre-fix filter reach onto a host lane (attachments/t_27c73d58/pod-personal-with-id.log; probe
attachments/t_27c73d58/probe-pod-personal.mjs, sha256 fa8decdb…, same bytes both arms):
  brain: createPodProfile('demo') → registered lanes self / workspace / project:default /
         pod/demo/personal / pod/demo/workspace; one extractable observation in each
  PRE-FIX unscoped owner REFLECT   → 1 claim / 1 page, in pod/demo/personal;
                                     entry: scope "personal", claims_created 1
  FIX unscoped owner REFLECT       → 5 claims / 5 pages, one per lane; entry: scope null,
                                     claims_created 5
```

The same probe run **without** an `operation_id` (the probe's earlier revision, sha256 `383538b0…`)
reproduces the claims/pages halves exactly (`attachments/t_27c73d58/pod-personal-measurement.log`, 1 claim pre-fix vs
5 post-fix) and shows no `reflect.explicit` entry is written at all — the entry is gated on a
caller-supplied id, as for every other verb, so the recorded lane is a property of the *call*, not of the
compile.

## Resulting state

- `wip/tech-head/reflect-noscope-all-scopes` @ the fix commit, stacked on `wip/neo/consent-change-scope`
  @ `658c3cb`, pushed as a PR against that branch (PR number, head SHA and CI conclusions are read from
  the PR — a revision named here would re-stale).
- The behaviour change above is the one call the card attaches to the merge: the PR is for review, not
  merged from this lane, and claim production on an owner-gated path moved.
- `test/protocol/reflect-no-scope-all-scopes.test.ts` fails on the pre-fix revision (3 of 6) and passes
  at the fix, with the same bytes on both arms, so a future re-introduction of a lane sentinel under the
  no-scope path cannot pass quietly.
- The `personal` literal no longer appears in `src/protocol/reflect.ts`; the remaining sites are the
  ones carded from `t_e6fce49a` (`session.ts:205`, `layer1/confidence.ts:6`,
  `protocol/forget_scope.ts:170`'s registry fallback, the `wiki/personal` directory, the OBSERVE
  tool-description example) — untouched here.
- `docs/conformance-status.md` gains a verified-baseline paragraph for this lane; `docs/STATUS.md` is
  regenerated after the docs commit (the generator reads HEAD).

## Unresolved

- **The owner call.** Requirement 2 of the card: the merge is the owner's, because claim production on
  the owner-gated path changes (0 → every lane for an unscoped run) and because an `operation_id` already
  used by an unscoped run **re-runs the compile on an existing brain** while its result reports the
  recorded claim count (corrected 2026-09-18 — see *Idempotency* above for the measurement; the
  replay-reporting half is carded as `t_efa8d5a8` / PR #32, not fixed on this lane). Both effects are
  visible at a merge, which is why the call is the owner's.
- `src/protocol/session.ts:205`'s `requested_scopes[0] ?? 'personal'` is the same class of literal on a
  different surface (a session created with `requested_scopes: []` hands the host's `onSummarize`
  callback the lane `personal`). Carded; not this card's site and not fixed here.
- `layer1/confidence.ts:6`'s `'personal': 365` half-life key still applies a 365-day half-life to a lane
  no brain registers (measured on `t_e6fce49a`: `personal` 0.4018 vs `self`/`workspace` 0.3305), while
  the spec's lane decays at the 90-day default. Carded; changing it moves canonical-ish values.
- The L2 page count for an unscoped compile moved with claim production (0 → 3 here) because pages are
  synthesized from claims; the compiler's own gather was already unscoped, so nothing else about the
  L2 stage changed. If a host wants "pages for everything, claims for one lane", that is a new
  parameter (or a new verb), not this fix.
