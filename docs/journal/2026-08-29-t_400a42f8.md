---
task: t_400a42f8
board: smartware
status: done
completed: 2026-08-29T15:08:25.000Z
created: 2026-08-29T11:13:56.000Z
assignee: "tech-head"
created_by: "user"
artifacts: ["/opt/data/dev-workspaces/repos/smartware/docs/competitive/coffee-tenant-config.example.json"]
---

# G2: clients-as-scopes config shape (client:<id> under workspace, visibility_default scope, staff grant clusters)

## Intent

Config shape for the Coffee tenant model: one business = one tenant; owner=owner_id; clients as scopes (client:<id> under workspace, visibility_default: 'scope' — NOT subjects); staff = Grant capabilities per client-scope cluster; non-reusable client:<id>#n markers. Lands WITH the FORGET.SCOPE cut (v0.5.0), same row as the indexes. Zero protocol changes — config shape only.

## Resulting state

Bound the Coffee tenant config shape in spec §10b (v0.8 NORMATIVE, commit f91a0a8) and plan v2: one business = one Pod; client:<id> scopes under workspace with visibility_default 'scope'; staff = Grant capability clusters as exact scope-id lists; non-reusable client:<id>#n markers. Code-verified against dist v0.6.3 (scripts/verify-config-shape.mjs) — zero config-code changes needed; the one seam i

**Artifacts recorded:**

- `/opt/data/dev-workspaces/repos/smartware/docs/competitive/coffee-tenant-config.example.json`

## Recorded notes

- **tech-head** (2026-08-29 15:08Z): Config shape DONE — bound in spec §10b (v0.8 NORMATIVE, commit f91a0a8). Key code-verified facts for downstream cards: 1. ZERO config-code changes needed; the shape round-trips through loadConfig/saveConfig today (verified against dist v0.6.3, harness scripts/verify-config-shape.mjs). 2. CLUSTER RULE (do not miss): scopeMatches supports exact ids, `*`, and `prefix/*` (slash) ONLY. `client:*`, `cli

## Provenance

- Canonical record: kanban board `smartware`, task `t_400a42f8` (`task_events` append-only log).
- Actors recorded: tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
