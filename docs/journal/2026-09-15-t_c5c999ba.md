---
task: t_c5c999ba
board: smartware
status: done
completed: 2026-09-15T08:14:13.000Z
created: 2026-09-14T18:16:48.000Z
assignee: "smarty-pants"
created_by: "neo"
artifacts: []
---

# DECIDE (+ maybe implement): explicit legal-hold marker — erasure refused under hold, sweep skip (ADR-0001 AC5–AC6 vs §10c.7 v1 composition)

## Intent

Surfaced by the P1 lifecycle verification card t_ad51d0e2 (branch wt/t_ad51d0e2, test/conformance/q_lifecycle_composition.test.ts C8).
**The gap.** ADR-0001 (owner-signed, Tier-1 invariants) says expiry/purge "never fires under a legal hold or open dispute", and its acceptance criteria 5/6 read "erasure ... refused under hold" and "hold scope is skipped by the sweep". §10c.7 of the Coffee binding defers the *explicit legal-hold marker* to G4 with "v1 = composition (offboarding + export + attestation)" — and no substrate hold state exists today. Verified this run: the sweep is tombstone-only (non-destructive) and the hold lane is offboarding + snapshot, so the *composition* holds; but nothing makes `FORGET.SCOPE { reason: 'erasure' }` refuse while a hold is open, and `expireRetention` has no held-scope skip.
**What this card decides.** Whether to keep v1 composition as the final answer (with the owner attestation now recorded server-side, `details.attestation`, t_ad51d0e2) or to add an explicit marker (e.g. `config.holds` / scope state) so that: (a) erasure on a held scope is refused, (b) the retention sweep skips held scopes, (c) hold release is an audited owner act. That is a protocol/config surface change requiring owner sign-off.
**Definition of done for this card.** One of: (1) a short decision record (ADR or §10c amendment) that v1 composition IS the answer, with the attestation receipt as the enforceable seam, and ADR-0001's AC5/AC6 wording reconciled; or (2) if the marker is chosen, a spec/ADR + failing tests first, then the minimal implementation (refusal + skip + release receipt) on a fresh branch off the lane tip, keeping idempotency/payload identity intact.
Evidence to start from: docs/competitive/mem0-substrate-spec-draft.md §10c.3/§10c.7 + change history

_(excerpt — first 15 lines. Full brief lives in the canonical board record: task `t_c5c999ba`.)_

## Resulting state

Owner (Stevie) ruled option (a): accept v1 composition (ADR-0008 option 1), card the explicit marker as pre-production. Decision was already implemented — ADR-0008 + conformance tests R1-R3, gates green per worker + reviewer (tech-head).

## Recorded notes

- **tech-head** (2026-09-14 18:26Z): Review trail (recorded before the terminal transition, so nothing is lost): - Review was requested from **smarty-pants** (author of ADR-0001, the owner-signed doc this ADR interprets) with three attack surfaces: (1) is the AC5/AC6 interpretation fair to invariant §2.2(3) or does it quietly narrow it? (2) is trigger-gated deferral right vs deriving a refusal from the log (ADR-0008 alternative 2, re

## Provenance

- Canonical record: kanban board `smartware`, task `t_c5c999ba` (`task_events` append-only log).
- Actors recorded: tech-head
- Generated by `scripts/substrate-journal-sync.mjs` — regenerate the projection, never this entry.
